# 스킬 마찰 로그 — 한 줄 누적

> **무엇을 적나**: 스킬·규칙을 쓰다 겪은 마찰 전부 — **막혔다/우회했다부터 '매끄럽지 않았다'까지 이 파일이 유일한 창구다**(킷개선 라벨 카드 채널 폐지 — 2026-08-09 오너 결정). 한 줄이면 된다 — 노이즈 환영, 판단은 나중에.
> **양식**: `YYYY-MM-DD /스킬 — 무엇이 (한 줄)` — **막힘·우회는 줄 머리에 `[막힘]` 태그**(즉시 수리 후보). 각 스킬 마무리 때 해당하면 append, 진행 중 커밋에 동봉(별도 커밋·카드 불요). 급한 건은 사용자에게 킷 세션 전달용 메시지 블록을 제시해도 되나 **기록의 정본은 이 파일**(채팅은 다음 세션이 못 읽는다).
> **★줄을 닫는 건 수리한 쪽이다**(2026-08-27 변경): `.claude/rules/*`·`.claude/skills/*`를 고치는 커밋은 해당 줄 끝에 **그 커밋 해시를 붙여 같은 커밋에 동봉**한다. 그래야 남은 `[막힘]`이 "아직 아무도 안 고친 것"만 가리킨다.
> 왜 바꿨나: 옛 규약("주 1회 사람 판단")은 **주체·트리거가 없어 실제로 안 돌았다** — 이 파일을 건드린 커밋 71건 중 판정 표기는 3건, 마지막이 2026-08-14였다(bnsone 세션 실측). 미반영 19건 중 **7건은 이미 고쳐졌는데 줄만 안 닫힌 것**이었다.
> **수리 우선순위**: `[막힘]` = 즉시 수리 후보. 그 외는 같은 것이 **두 번** 나오면 수리 후보(한 번은 우연, 두 번은 패턴). 나머지는 기록만.

---
2026-08-27 /code-review — session-guard 훅이 `cd A && git -C B checkout` 을 놓쳤다(대상 판정이 cd 우선). 같은 리뷰에서 rebase 문서 누락·pid 미확인 시 자기 점유 오경고까지 3건 — 그 자리에서 수리·회귀 케이스 3건 추가. → 수리 `908d15b`
2026-08-27 /code-review — session-guard 2차 지적 2건: 뒤에 붙은 `cd` 가 대상 판정을 뒤집어 무음 · 남의 잠금 폴더가 쓰기 불가면 점유가 통째로 무음. → 수리 동봉(이 커밋)
2026-08-27 /done — check-density 가 한글을 **바이트로** 셌다(맥 awk: 10자→30). 상한이 3배 엄해져 지킬 수 없는 줄이 "옛 초과"로 쌓였고 아무도 그 숫자를 의심하지 않았다. 회귀 테스트가 없어 오래 살아남았다(bnsone 세션 발견). → 계수 수리 + `test-check-density.sh` 신설(이 커밋)
2026-08-27 CI — 검사기 4종의 회귀 테스트가 **CI에서 하나도 안 돌고 있었다**(로컬 전용). 게다가 밀도 테스트는 CI 변수(`CI_MERGE_REQUEST_DIFF_BASE_SHA`)가 있으면 전수검사 폴백으로 떨어져 핵심 분기를 못 봤고, 세션가드 테스트는 컨테이너에 claude 조상이 없어 4건이 환경 탓으로 빨개졌다(bnsone 세션 지적에서 출발). → `self-tests` 잡 신설(차단)·테스트 2건 환경 의존 제거(이 커밋)
2026-08-27 /code-review — session-guard 3차 지적 2건: 경고문에 보간되는 값(경로·브랜치명)에 따옴표가 있으면 JSON 이 깨져 **판정이 통째로 버려진다**(git 은 `feat"quote` 를 허용한다 — 실측) · 훅 임시본이 gitignore 밖이라 잔해가 추적된다. → 수리 동봉(이 커밋)
2026-08-28 /done·/dev — 한 카드에서 게이트가 8회 섰는데 답이 산출물을 바꾼 건 2회뿐이었다(bnsone #159 실측, 오너 "도장찍기"). 정지가 잦을수록 요약이 길어지고 길수록 안 읽혀 «가시성» 목적이 정반대로 무너진다. → 고위험 라벨을 묶음 게이트 배제에서 제외 · /done 공지·push·머지를 묶음 승인 1회로 · 요약 서식 강제(이 커밋)
2026-08-28 /ui — 정본 문서를 만들라는 규정은 있는데 «만든 뒤 자동으로 읽히게 하라»는 규정이 없다. bnsone 에서 디자인 토큰 정본 67줄이 있는데 세션이 "없다"고 답해 오너가 잘못된 결론에 도달했다(유일한 연결이 /ui SKILL 예시 경로 한 줄 → /ui 를 안 부르면 존재를 모른다). → CLAUDE.md 공통 규칙 신설 + /ui 0단계 보강(이 커밋)
2026-08-28 규칙 로딩 — `paths:` 조건 규칙이 **셸(cat/sed)로 읽을 땐 로드되지 않는다**(격리 프로젝트 + InstructionsLoaded 훅 로그로 실측). Bash 우선으로 읽는 세션에서는 migrations·shared·docs·module-* 규칙이 통째로 무음이 된다. CLAUDE.md 가 migrations.md 를 "상시 로드"라고 잘못 적고 있던 것도 이 과정에서 드러났다. → 정정 + 규칙 신설 + 근거 `_reference/rules-loading.md`(이 커밋)
2026-08-28 규칙 배치 — 위 실측을 근거로 조건부 규칙을 층별로 재배치했다: `migrations.md`(스키마가 깨진다)·`shared.md`(전원이 깨진다)는 `paths:` 제거해 항상 읽히게, `docs.md`·`module-*.md`는 조건부 유지하되 한계를 파일 머리에 명시. **옮긴 뒤 «실제로 걸리는지»도 나란히 재서 확인**(조건부 안 걸림 / 무조건 걸림). → 이 커밋
