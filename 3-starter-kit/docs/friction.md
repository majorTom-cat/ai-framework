# 스킬 마찰 로그 — 한 줄 누적

> **무엇을 적나**: 스킬·규칙을 쓰다 겪은 마찰 전부 — **막혔다/우회했다부터 '매끄럽지 않았다'까지 이 파일이 유일한 창구다**(킷개선 라벨 카드 채널 폐지 — 2026-08-09 오너 결정). 한 줄이면 된다 — 노이즈 환영, 판단은 나중에.
> **양식**: `YYYY-MM-DD /스킬 — 무엇이 (한 줄)` — **막힘·우회는 줄 머리에 `[막힘]` 태그**(즉시 수리 후보). 각 스킬 마무리 때 해당하면 append, 진행 중 커밋에 동봉(별도 커밋·카드 불요). 급한 건은 사용자에게 킷 세션 전달용 메시지 블록을 제시해도 되나 **기록의 정본은 이 파일**(채팅은 다음 세션이 못 읽는다).
> ★**적은 줄은 «마찰 수거» 카드 `#<수거카드번호>` 에 `@<공통개발자>` 멘션 댓글 한 줄로 알린다**(카드가 있는 repo. 없으면 이 자리에 «없음»이라 적는다): 이 파일은 **아무 기계도 내용을 안 읽는다** — CI·훅·스크립트 전수에서 파싱 **0건**(2026-09-04 실측, 킷·배포처 동일). 그래서 `[막힘]`은 담당자·라벨이 없어 **소유자에게 안 간다**(체크박스·라벨 변경은 GitLab 알림 자체가 없다 — 2026-08-19 실측). **수리해서 줄을 닫는 커밋도 같은 카드에 한 줄**을 남긴다. 카드는 닫지 않는다(상설).
> ★**집을 때도 한 줄 남긴다 — 「이건 제가 집습니다」.** 적는다 → **집는다** → 닫는다 세 박자다. 이 한 칸이 없으면 **두 사람이 같은 줄을 동시에 붙잡는다**(2026-09-04 실측: 아침에 적힌 3건을 오후에 다른 사람이 손댔고, 소유자는 **다 고쳐진 뒤에** 알았다 — 그게 이 카드를 만든 사고다). 집었는데 못 끝냈으면 그것도 한 줄로 알려라.
> **★줄을 닫는 건 수리한 쪽이다**(2026-08-27 변경): `.claude/rules/*`·`.claude/skills/*`를 고치는 커밋은 해당 줄을 **같은 커밋에서 닫는다**. 그래야 남은 `[막힘]`이 "아직 아무도 안 고친 것"만 가리킨다.
> **★닫는 표기는 줄 끝 `[닫힘:…]` 한 형태뿐이다**(2026-08-31 신설). 기계가 보는 건 **`[닫힘:` 접두어**이고, 안에 넣는 값은 셋 중 하나다: **`#카드번호`**(같은 repo 수리 — 권장) · **커밋해시**(킷동기처럼 «다른 repo 의 커밋», **또는 같은 repo 의 «이미 지나간» 커밋**을 가리킬 때 — 적으면서 같은 커밋에서 고친 옛 줄이 여기 해당한다. 단 **지금 만드는 커밋의 해시는 안 된다**, 그건 `이 커밋`) · **`이 커밋`**(카드가 없는 repo 에서 같은 커밋으로 닫을 때) · **`구기록`**(2026-08-31 이전에 자유서술로 이미 닫힌 줄) · **`기록만`**(판정 결과 «고칠 것이 아니다» — 규칙이 의도대로 작동한 사례, 한 번뿐이라 아직 패턴이 아닌 것. 2026-09-07 신설: 이 값이 없으면 **판정이 끝난 줄이 영영 «안 닫힌 줄» 수에 남아** 그 숫자가 죽는다).
> ★**옛 줄은 «그대로 두지» 말고 한 번에 닫아라**: 토큰 신설 전 줄이 남아 있으면 0-c 스캔이 **매 세션 같은 수를 뿜어 아무도 안 읽게 된다**(2026-08-31 실측: bnsone 32건 — 열어 보니 32건 전부 «킷 반영 완료»·«해소 확인»으로 이미 닫힌 줄이었다). 토큰을 처음 들이는 repo 는 **그 커밋에서 옛 줄을 훑어 닫힌 것에 `[닫힘:구기록]`을 붙이고, 스캔이 0인지 돌려 확인**한다.
> ⚠️**같은 repo 수리를 자기 커밋 해시로 닫지 마라 — 자기 참조다**: 해시는 커밋해야 생기는데 넣고 amend 하면 그 해시가 또 바뀐다(bnsone 첫 사용에서 바로 걸렸다). 자유서술로 닫으면 **기계로 셀 수 없다**: bnsone 이 미수리 건수를 세 번 세어 23·17·19 세 값을 얻었고(키워드가 양쪽으로 틀린다 — 「해소」가 문제 서술에도 나온다) 20줄을 전부 열어야 실수를 잡았다. 토큰이 있으면 `grep -c '\[막힘\]' | grep -v '\[닫힘:'` 한 줄로 끝난다.
> **★킷 → 프로젝트 동기 커밋도 닫는다**: 배포본에 같은 취지의 줄이 있으면 `킷동기:` 커밋이 그 줄을 함께 닫아라. 안 닫으면 «이미 고쳐졌는데 줄만 열려 있는» 건이 쌓여 우선순위 판단이 죽는다(2026-08-31 실측: 미수리로 세던 20건 중 1건이 그것 — `/overhaul` 범위 확인 건이 킷동기 `d8ec55f`로 이미 들어와 있었다).
> 왜 바꿨나: 옛 규약("주 1회 사람 판단")은 **주체·트리거가 없어 실제로 안 돌았다** — 이 파일을 건드린 커밋 71건 중 판정 표기는 3건, 마지막이 2026-08-14였다(bnsone 세션 실측). ★**그 대체물(`/todo` 0-c 스캔)도 혼자서는 부족했다**: 2026-09-04 에 아침 브리핑에 뜬 막힘 3건이 번호·담당자 있는 카드들에 밀렸고, 오후에 남이 고친 뒤에야 소유자가 알았다 — **세션을 연 사람에게만 보이기 때문**이다. 그래서 «수거 카드 멘션»을 정본 알림으로 두고 0-c 는 백스톱으로 남긴다. 미반영 19건 중 **7건은 이미 고쳐졌는데 줄만 안 닫힌 것**이었다.
> **수리 우선순위**: `[막힘]` = 즉시 수리 후보. 그 외는 같은 것이 **두 번** 나오면 수리 후보(한 번은 우연, 두 번은 패턴). 나머지는 기록만.

---
2026-08-27 /code-review — session-guard 훅이 `cd A && git -C B checkout` 을 놓쳤다(대상 판정이 cd 우선). 같은 리뷰에서 rebase 문서 누락·pid 미확인 시 자기 점유 오경고까지 3건 — 그 자리에서 수리·회귀 케이스 3건 추가. → 수리 `908d15b`
2026-08-27 /code-review — session-guard 2차 지적 2건: 뒤에 붙은 `cd` 가 대상 판정을 뒤집어 무음 · 남의 잠금 폴더가 쓰기 불가면 점유가 통째로 무음. → 수리 동봉(이 커밋)
2026-08-27 /done — check-density 가 한글을 **바이트로** 셌다(맥 awk: 10자→30). 상한이 3배 엄해져 지킬 수 없는 줄이 "옛 초과"로 쌓였고 아무도 그 숫자를 의심하지 않았다. 회귀 테스트가 없어 오래 살아남았다(bnsone 세션 발견). → 계수 수리 + `test-check-density.sh` 신설(이 커밋)
2026-08-27 CI — 검사기 4종의 회귀 테스트가 **CI에서 하나도 안 돌고 있었다**(로컬 전용). 게다가 밀도 테스트는 CI 변수(`CI_MERGE_REQUEST_DIFF_BASE_SHA`)가 있으면 전수검사 폴백으로 떨어져 핵심 분기를 못 봤고, 세션가드 테스트는 컨테이너에 claude 조상이 없어 4건이 환경 탓으로 빨개졌다(bnsone 세션 지적에서 출발). → `self-tests` 잡 신설(차단)·테스트 2건 환경 의존 제거(이 커밋)
2026-08-27 /code-review — session-guard 3차 지적 2건: 경고문에 보간되는 값(경로·브랜치명)에 따옴표가 있으면 JSON 이 깨져 **판정이 통째로 버려진다**(git 은 `feat"quote` 를 허용한다 — 실측) · 훅 임시본이 gitignore 밖이라 잔해가 추적된다. → 수리 동봉(이 커밋)
2026-08-28 /done·/dev — 한 카드에서 게이트가 8회 섰는데 답이 산출물을 바꾼 건 2회뿐이었다(bnsone #159 실측, 오너 "도장찍기"). 정지가 잦을수록 요약이 길어지고 길수록 안 읽혀 «가시성» 목적이 정반대로 무너진다. → 고위험 라벨을 묶음 게이트 배제에서 제외 · /done 공지·push·머지를 묶음 승인 1회로 · 요약 서식 강제(이 커밋)
2026-08-28 /ui — 정본 문서를 만들라는 규정은 있는데 «만든 뒤 자동으로 읽히게 하라»는 규정이 없다. bnsone 에서 디자인 토큰 정본 67줄이 있는데 세션이 "없다"고 답해 오너가 잘못된 결론에 도달했다(유일한 연결이 /ui SKILL 예시 경로 한 줄 → /ui 를 안 부르면 존재를 모른다). → CLAUDE.md 공통 규칙 신설 + /ui 0단계 보강(이 커밋)
2026-08-28 규칙 로딩 — `paths:` 조건 규칙이 **셸(cat/sed)로 읽을 땐 로드되지 않는다**(격리 프로젝트 + InstructionsLoaded 훅 로그로 실측). Bash 우선으로 읽는 세션에서는 migrations·shared·docs·module-* 규칙이 통째로 무음이 된다. CLAUDE.md 가 migrations.md 를 "상시 로드"라고 잘못 적고 있던 것도 이 과정에서 드러났다. → 정정 + 규칙 신설 + 근거 `_reference/rules-loading.md`(이 커밋)
2026-08-28 규칙 배치 — 위 실측을 근거로 조건부 규칙을 층별로 재배치했다: `migrations.md`(스키마가 깨진다)·`shared.md`(전원이 깨진다)는 `paths:` 제거해 항상 읽히게, `docs.md`·`module-*.md`는 조건부 유지하되 한계를 파일 머리에 명시. **옮긴 뒤 «실제로 걸리는지»도 나란히 재서 확인**(조건부 안 걸림 / 무조건 걸림). → 이 커밋
2026-08-28 조회 습관 — `grep -A N`·`head` 로 **잘린 목록**을 보고 「없다」를 단정하는 사고가 하루 2회(bnsone 실측). CLAUDE.md 금지 절은 「직접 조회 없이 단정 금지」만 말하고 **「잘린 조회는 조회가 아니다」는 안 말했다.** 같은 날 「색인 + 상세」 이중화가 드리프트 경고 대상인지도 규약에 없어, 다음 사람이 «중복이네» 하고 항상 읽히는 쪽을 지울 위험이 있었다. → 둘 다 규칙으로 명시(이 커밋)
2026-08-31 세션 시작 — session-guard 「중복 세션」 경고가 첫 응답을 브리핑 대신 «도장찍기 정지»로 만들었다(bnsone 실측). SessionStart 훅 2개가 같은 턴에 「바로 시작하라」와 「확인받아라」로 충돌 — 규칙 문구가 「확인받아라」라 정지가 이겼고, 물어본 뒤 돌린 /todo 는 미완 0·멘션 0이었다(정보량 0인 정지). 실제 위험은 조회가 아니라 HEAD 이동인데 정지가 **위험 지점이 아니라 세션 시작 지점**에 걸려 있었다. → 이 커밋 [닫힘:f43d667]
2026-08-31 /dev·/fix — [막힘] 자동 조회 한 줄이 **스킬 실행 자체를 막았다**(bnsone 실측). frontmatter `` !`case … esac` `` 이 `allowed-tools` 에 없는 `echo`·`case` 를 섞어 거부 → 본문이 뜨지 않고, 두 스킬은 `disable-model-invocation: true` 라 **개발자 1명이 어떤 카드도 착수 못 했다**(3회 연속). 거부 사유는 «위험한 문법»이 아니라 «선언한 명령만 쓰는가»다 — `grep` 으로 바꿔도 똑같이 막혔다. ★같은 파일이 27일간 잘 돌았다: **갈린 건 사람이 아니라 세션의 권한 모드**이고 하네스 업데이트로 언제든 엄격해진다. → 4줄을 «단일 glab + 파이프»로 바꾸고 인자 판정을 본문 문장으로 옮김 + CLAUDE.md 규칙 신설 [닫힘:이 커밋]
2026-08-31 /todo — [막힘] 새로 만든 0-c 스캔이 **배포처에서 상수 오탐**이었다. 킷에서만 «0건»을 확인하고 끝냈는데, bnsone main 에 그대로 돌려 보니 **32건**이 뜬다 — 32건 전부 「킷 반영 완료」·「해소 확인」으로 **이미 닫힌 옛 줄**이었다(`[닫힘:` 토큰 신설 전이라 기계가 못 알아본다). 원인은 규칙 자체: friction 머리에 «그 전 줄들은 자유서술이라 **그대로 둔다**»라고 적어 둔 것 — 킷은 옛 줄이 1개뿐이라 안 드러났다. 「지금 값이 0인가」는 **킷이 아니라 배포처에서** 돌려야 잡힌다. → 수리: 닫는 값에 `구기록` 추가 + 「옛 줄은 한 번에 닫아라」 + /todo 0-c 에 «상수면 경보가 아니라 고장» [닫힘:이 커밋]
2026-09-01 킷 ← bnsone 역이관 — 배포처가 자체 수리한 3곳(이슈 `description` 갱신법 · CI 조건 분기는 스크립트로 빼고 자기테스트 · 로그인이 안 되면 «앱 고장» 아니라 «자격증명 없음»부터)과 아직 아무도 안 고친 마찰 7건을 킷 정본에 반영했다. ★**배포처에서만 드러나는 함정이 많다** — 킷은 데이터·사용자가 적어 «돌려 보면 안 나오는» 것들이다(오늘 0-c 스캔 건과 같은 뿌리). 미채택 1건 = `check-failclosed` 헛FAIL(그 검사기는 킷에 없다) — 대신 «검사기 헛FAIL 은 검사기 약화 압력을 만든다»만 `/dev` 6단계에 넣었다. [닫힘:이 커밋]
2026-09-01 /todo — [막힘] To-Do 자동 조회가 **매 세션 문맥에 18만 자를 붓고**, 뜨는 목록의 76%가 이미 끝난 일이었다(실측: `glab api todos` 응답 **179,951자** · 대기 29건 중 **22건**이 닫힌 카드·머지된 MR, 최고령 33일). `| head -60` 은 응답이 **개행 없는 한 줄**이라 아무것도 못 자른다. GitLab To-Do 는 스스로 안 사라지는데 **아무 스킬도 지우라고 안 적어 놨다** — 0-c 와 같은 «상수로 뜨는 목록은 안 읽힌다» 고장이다. → 조회를 `glab todo list`(표·5,144자)로 바꾸고 0-d「끝난 To-Do 비우기」신설 [닫힘:이 커밋]
2026-09-01 /todo — [막힘] 바로 앞 커밋(`8ccbf4b`)에서 바꾼 `glab todo list` 가 **기본 30건에서 조용히 잘렸다**(실측: 대기 31건인데 기본 조회는 30건만 준다 — `-P 100` 을 붙여야 전부 온다). 배포처 MR 을 올린 뒤 승인 직전에 발견했다. **«문맥을 줄인다»는 수리가 «부재를 단정하는 잘린 조회»를 새로 만들 뻔했다** — 킷이 금지 절에 이미 「잘린 조회로 없다를 단정 금지」를 적어 두고도 그 조회를 내가 새로 넣었다. ★교훈: **`head`·기본 per-page 처럼 «자르는 것»은 새 조회를 넣을 때마다 그 자리에서 한계값을 재라**(둘 다 이번 한 파일에서 나왔다). → `-P 100` 추가 + 빼지 말라는 경고 [닫힘:이 커밋]
2026-09-01 킷동기 — [막힘] 배포처 MR 2건이 `high-risk-gate` 에서 튕겼다: **`셀프승인` 라벨이 없고, 라벨은 파이프라인 «생성» 시점에만 읽힌다**(붙이고 잡만 재실행하면 반영 안 됨). 절차 자체는 `/done` `함정.md:23` 에 정확히 적혀 있었는데 **동기 MR 은 /done 을 안 거치므로 그 문서를 열 이유가 없다** — 그래서 세션이 기억으로 승인 스크립트를 짜고 사람이 **명령을 두 번 치게 됐다**(오너 지적). ★교훈: **사람 손이 필요한 절차는 «그 절차를 부르는 스킬»에만 두면, 다른 경로로 온 사람은 못 찾는다** — 항상 읽히는 층에 포인터 한 줄이 필요하다. → CLAUDE.md 킷동기 예외 줄 밑에 포인터 추가 [닫힘:이 커밋]
2026-09-04 킷개선 — [막힘] `docs/friction.md` 의 «발견»이 **아무에게도 안 갔다**. bnsone 오너 질문: 「공통 개발자가 해야 할 일을 왜 다른 개발자가 고칠 때까지 내가 몰랐지?」 — 그날 아침 두 세션이 `[막힘]` 3줄(전부 킷 소유 = 오너 몫)을 적었고, 오후에 모듈 소유자가 통째로 고쳐 머지했으며, **오너는 그 머지 알림으로 처음 알았다**. 원인은 셋: ①이 파일 **내용을 읽는 기계가 0건**(CI·훅·스크립트 전수 — bnsone 실측, 킷도 동일 확인) ②`[막힘]`은 담당자·라벨이 없어 **멘션·카드 조회에 안 걸린다**(규칙대로 — 카드 채널 폐지 2026-08-09) ③유일한 노출인 `/todo` 0-c 는 **세션을 연 사람에게만** 보여, 번호·담당자 붙은 카드들에 밀렸다. 반대로 «해결»은 규칙 변경 커밋이 알림을 타므로 **«발견은 무음, 해결만 알림»** 구조였다. ★교훈: **주기적으로 사람이 읽는 규약은 두 번 다 안 돌았다**(08-12 «주 1회 회고» 6일 방치 · 08-31 막힘 3건 3~5일 방치) — 알림을 타는 곳에 얹어야 한다. ★함정: bnsone 이 제안한 «체크박스로 얹기»는 그대로 쓰면 또 무음이다 — **체크박스·라벨 변경은 GitLab 알림 자체가 없다**(2026-08-19 실측)는 킷 자신의 값과 충돌한다. 그래서 **담당자 있는 카드 + 멘션 댓글**로 짰다. → repo 당 «마찰 수거» 카드 1장 신설(setup-gitlab 3-b), 적을 때·닫을 때 멘션 댓글 규칙(CLAUDE.md·/dev·/fix·friction 머리), 0-c 에 «며칠째·소유자 몫·멘션 확인» 추가, Leader 가이드에 남아 있던 폐기 규약(«주 1회 회고») 정리 [닫힘:이 커밋]
2026-09-04 킷동기 — [막힘] 배포처 동기 MR 이 **킷에 없는 «배포처 전용 게이트»** 에서 튕겼다: bnsone `change-summary` 는 규칙 파일(`CLAUDE.md`·`.claude/**`·`docs/00_Guide/**`·`.gitlab-ci.yml`)을 바꾸는 MR 의 **마지막 커밋** 메시지에 「달라지는 것:」 블록을 요구한다. 킷 CI 에는 그 잡이 없어 세션이 **존재 자체를 몰랐다**. 2026-09-01 고위험 게이트 건과 **같은 모양**(절차가 배포처 안에만 있고 킷동기 경로는 그 문서를 열 이유가 없다)인데, 이번엔 «절차가 아예 다른 repo 에 있다»가 더해졌다 — 킷 기준으로 초록을 예상한 게 틀렸다. ★교훈: **배포처에 MR 을 올리기 전에 그 repo 의 `.gitlab-ci.yml` 잡 목록을 먼저 봐라.** 덧: 그 잡은 막으면서 「새 커밋 하나를 더하면 된다(force push 불요)」까지 알려줘서 한 번에 풀렸다 — **안내 있는 차단**이 사람 손을 한 번 아꼈다. → CLAUDE.md 킷동기 절에 한 줄 [닫힘:이 커밋]
2026-09-04 킷개선 — 위 «마찰 수거 카드» 규칙이 **실제로 알림을 만드는지 그 자리에서 쟀다**(만든 날 첫 사용). ★확인해야 했던 것: 킷 세션은 **오너와 같은 계정으로 돈다** — 자기 자신을 멘션하면 GitLab 이 알림을 안 줄 수 있고, 그러면 규칙이 «1인 상황에서만 죽는» 물건이 된다(오늘 사고가 정확히 그 상황이었다: 적은 것도 AI 세션, 몰랐던 것도 오너). 실측: bnsone #214 에 `@yskim` 닫음 댓글 1건 → To-Do `941 directly_addressed` 가 **새로 생겼다**(카드 배정만으로도 `938 assigned` 가 이미 있었다). **자기 멘션도 To-Do 를 만든다 — 규칙은 1인 상황에서도 작동한다.** ⚠️안 잰 것: 팀 채팅(Mattermost)까지 가는지는 확인 안 했다 — GitLab 통합 설정에 달렸고, 이 값으로 단정하지 마라 [닫힘:이 커밋]
2026-09-07 킷개선 — [막힘] **오늘 만든 «마찰 수거 카드» 알림이 실제로 쌓이는 것의 대부분을 안 나른다.** 오너 질문에서 드러났다: 「3일이 지났는데 새로 올라온 게 없었어?」 — 0-c 가 «막힘 0건»이라 답했는데 **실제로는 bnsone 에 3일간 3줄이 새로 적혔고, 8-31 이후 안 닫힌 줄이 18건**이었다. `[막힘]` 태그는 «완전히 막혔을 때»만 붙는데 실제로 쌓이는 건 «불편했다»급이라 통째로 안 보였다. ★18건을 전수로 갈랐다: **기록만 1 · bnsone 몫 2 · 킷 소유 미수리 15**(«이미 고쳐놓고 줄만 안 닫은 것»은 **0건** — 8-31 실측에선 20건 중 7건이 그랬는데 이번엔 전부 진짜였다. 기대로 넘겨짚지 마라). 15건 중 **6건이 `/dev` 6단계 «돌연변이 검증» 한 대목에 몰려 있었다** — 한 곳에 여섯 번 걸렸다는 건 줄을 더 붙일 게 아니라 **다시 써야 한다**는 뜻이라, 절을 «①겨눈다 ②지운다 ③판정한다 ④되돌린다» 순서로 재편했다(기존 실측 예외는 하나도 안 지웠다). ★함께 드러난 것 둘: ⑴**bnsone 이 9-04 에 고친 킷 규칙(`-uno` 게이트·「겨눈 파일 의심」)이 역이관 안 돼 파일럿이 계속 밟고 있었다** — 8-12 교훈(「프로젝트에서 고친 버그는 킷으로 역류시켜라」)이 또 반복됐다 ⑵**「기록만」으로 판정한 줄을 닫을 값이 없어** 영영 «안 닫힌 줄»에 남는다 → `[닫힘:기록만]` 신설. → 0-c 에 «안 닫힌 줄» 값 추가 + 「집는다」 박자 신설 + 15건 수리 + 역이관 [닫힘:이 커밋]
2026-09-07 킷개선 — [막힘] 스킬 크기 기준 「13KB」가 **무엇을 재는 값인지 정하지 않아 나를 오도했다**. `wc -c`(바이트)로 재서 「/dev 25KB = 기준의 두 배」라 오너에게 보고했는데 **한글 1자가 3바이트**라 세 배로 부푼 값이었다(문자로는 13,003자 — 「13KB」를 문자로 읽으면 오히려 «딱 경계»가 된다). 결론은 우연히 맞았지만 근거가 틀렸다. → **토큰을 실측했다**: `claude -p --output-format json` 의 usage 합(input+cache_creation+cache_read)을 빈 프롬프트 기준선과 빼서 **12,072토큰** — 공식 한도(「컴팩션 시 스킬당 5,000토큰까지만 재부착」, `4-reference/claude-code-docs-analysis.md:66`)의 **2.4배**다. ★**한국어 1자 ≈ 0.93토큰**이라 「13KB ≈ 5천 토큰」은 영어 기준 환산이고 한국어 문서에선 2.4배 틀린다 → 기준을 «약 5,400자»로 바꾸고 재는 법 두 가지(문자 세기·토큰 실측)를 가이드에 적었다. ★함께 실증(가이드가 «분리 후 실증한 뒤에만»이라 요구한 것): 격리 프로브에서 「§2 를 지금 읽어라」(강한 지목)와 「참고 문서: 함정.md」(약한 지목) **둘 다** 보조 파일을 `cat` 으로 읽고 그 안의 규칙을 따랐다(마커로 판정). ⚠️단 n=1씩·스킬 폴더에 파일 2개뿐·짧은 세션·과제 1종이다 — **«지목은 없어도 된다»로 읽지 마라**(둘 다 `ls` 로 폴더를 먼저 훑고 찾았다. 파일이 많은 실제 스킬에서 같을지는 안 쟀다). → `/dev` 의 돌연변이 검증·커밋 함정을 `함정.md` 로 분리해 13,003→10,540자. ⚠️**아직 목표 미달(≈9,800토큰)** — 남은 본문은 함정이 아니라 절차라 더 뺄 «함정»이 없다. 맞추려면 절차 문장의 실측 근거 괄호를 걷어내는 본문 재작성이 필요한데, 되돌리기 어려워 오늘은 안 했다 [닫힘:이 커밋]
2026-09-07 킷개선 — [막힘] **보조 파일을 «만든다»고 생각한 순간 «이미 있나»를 안 봤다.** `/dev` 함정 분리를 하면서 `.claude/skills/dev/함정.md` 를 파이썬 `open(...,'w')` 로 통째로 썼는데 **그 파일은 이미 있었다**(2,809바이트·5개 절). 커밋 뒤 `git status` 가 `A`(추가) 아닌 **`M`(수정)** 인 걸 보고 알았다 — 그것 하나가 유일한 신호였다. 다행히 커밋돼 있어 `git show HEAD~1:` 로 전수 복원했고 **원본 21줄 중 사라진 줄 0개**를 대조로 확인했다. ★더 나쁜 게 하나 더 있었다: 새 절을 **§1·§2 로 번호 매겨** 붙였는데 본문은 이미 `함정.md §1`~`§5` 를 가리키고 있었다 — **깨진 링크가 아니라 «잘못된 링크»**라 아무 에러도 안 나고 조용히 엉뚱한 절로 보낸다. 게다가 내가 뺀 «구현» 조각은 원본 §5 가 이미 다루던 것이라 **멀쩡한 포인터를 개악**한 것이었다. → 새 절은 §6 으로 재배치하고 본문 포인터를 원복했다. ★교훈 둘: ⑴**보조 파일을 쓰기 전에 `ls`·`git status` 로 존재부터 확인하라** — Write 도구는 덮어쓰기를 경고하지만 **셸·파이썬 쓰기는 아무 말도 안 한다**(2026-09-01 실측 「Write 로 새 파일을 만들 때만 규칙이 안 걸린다」와 같은 결) ⑵**절 번호를 붙일 땐 그 파일을 가리키는 본문 참조를 먼저 grep 하라** — 번호 충돌은 무음으로 실패한다 [닫힘:이 커밋]
2026-09-07 킷개선 — [막힘] 「스킬이 크면 뒤가 잘린다」를 **재는 도구가 없어서 아무도 몰랐다.** 공식 동작을 재확인하니 컴팩션은 각 스킬의 **«앞 5,000토큰만 남기고 뒤를 자른다»**(드롭이 아니라 **절단** · 재부착 전체 합산 25,000토큰이고 **최근 호출 순**이라 한 세션에서 스킬을 많이 부르면 오래된 것은 통째로 빠진다 · ★**보조 파일은 이 예산에 안 든다** — 읽을 때만 로드). 실측하니 **4개 스킬이 초과**였고 `/dev` 는 한도의 1.7배라 **6단계부터 통째로 안 읽히고 있었다.** ★특히 `/todo` 는 **잘리는 뒤쪽이 더 중요했다** — 「선택지는 «무엇이 달라지나»로 적어라」(오너가 "무슨말이야" 2회 한 뒤 넣은 규칙)가 거기 있다. → `check-skill-size.sh` 신설: 단순 «크다»가 아니라 **어느 절부터 안 읽히는지 · 앞쪽에 보조 파일 안내가 있는지**까지 찍는다(「경고는 안 읽힌다」의 해법은 «경고를 유용하게»다). 자기테스트 10케이스 + `density-check` 잡에 편입. `/dev` 6·7단계를 `단계상세.md` 로 빼고 ★**보조 파일 안내를 맨 앞에** 뒀다 — **뒤에 두면 포인터째 잘려서 «어디를 읽어야 하는지»조차 안 남는다**(이게 이 건의 핵심 설계다). `/todo` 는 «잘려도 지킬 것» 두 줄을 앞으로 올렸다. ★단위 함정도 실측해 박았다: 문자 수는 **`wc -m`** 이고 **`awk length` 는 바이트다**(alpine busybox 같은 파일 9,232자 vs 18,328) — 자기테스트 C 가 그 회귀를 막는다(한글 5,008자 = 12,216바이트 파일이 통과해야 한다). ★후속: **넘는 4개 스킬 전부에 «잘려도 지킬 것» 한 줄을 앞쪽에** 넣고(보조 파일로 못 빼는 부분의 차선책 — `/dev` 는 «남의 모듈·main 직접 작업·게이트 건너뛰기 금지», `/done` 은 «카드를 Close 하지 마라», `/scaffold` 는 «설계 없이 스택 정하지 마라», `/todo` 는 «선택지를 사람 말로»), **검사기가 그 보강을 알아보게** 했다 — 안 그러면 «고쳤는데 같은 경고»가 계속 떠서 **결국 안 읽힌다**(경고 피로는 경고를 만든 쪽 책임이다). ★그 과정에서 검사기 메시지를 바꿨더니 **자기테스트 E1 이 그 자리에서 잡았다** — 검사기를 고칠 때 테스트가 같이 안 따라오면 무음으로 헐거워진다는 걸 실물로 보여준 셈이다 [닫힘:이 커밋]
2026-09-07 킷동기 — [막힘] **절을 통째로 옮길 때 «마지막 빈 줄»이 사라져, 다음 동기가 «다르다»고 멈췄다.** 원인은 내가 절을 뽑은 방법이다: `awk '/^## 6/,/^## 7/' | sed '$d'` — 끝 표지 줄을 지우려고 붙인 `sed '$d'` 가 **그 앞의 빈 줄까지** 먹었다(정확히는 마지막 한 줄만 지우므로 표지가 남고 빈 줄이 갔다). 그렇게 만든 문안이 배포처 2곳에 그대로 들어가, 다음 킷동기에서 «6·7단계가 킷 직전판과 다르다»로 안전장치가 걸렸다. ★**내용은 완전히 같았다** — 빈 줄 2개뿐이었다(3,149자 vs 3,147자, 빈 줄 빼면 일치). 안전장치가 «모르면 멈춘다»로 옳게 동작했지만, **원인이 내 추출 실수라 사람이 두 번 나설 뻔했다.** → 비교를 «빈 줄만 다르면 통과하되 그 사실을 출력»으로 완화했다(내용이 다르면 여전히 멈춘다). 교체하면 킷 문안이 들어가므로 빈 줄도 함께 복원된다. ★교훈 둘: ⑴**문서 조각을 잘라 옮길 땐 «줄 단위 셸 필터»(`sed '$d'`·`head -n`)를 쓰지 마라** — 눈에 안 보이는 빈 줄이 소리 없이 사라진다. **인덱스 슬라이스**(시작·끝 표지의 위치를 찾아 그 사이)가 안전하다 ⑵**동기 스크립트의 «다르면 멈춤»은 내용 기준이어야 한다** — 공백까지 엄격하면 자기가 만든 흔적에 자기가 걸린다 [닫힘:이 커밋]
