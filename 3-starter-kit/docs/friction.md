# 마찰 로그 — ★최신이 맨 위

> **적는 법**: `YYYY-MM-DD /스킬 — 무엇이 (한 줄)` 을 **아래 «지금 살아 있는 것» 맨 위**에 넣는다(끝에 붙이지 마라). 막힘·우회는 줄 머리에 **`[막힘]`**. 스킬 마무리 때 그 커밋에 동봉 — 별도 커밋·카드 불요.
> **닫는 법**: 고친 커밋이 그 줄 끝에 **`[닫힘:…]`** 을 붙인다. ⚠️기계가 보는 건 **`[닫힘:` 접두어뿐** — `[닫힘(①만):` 처럼 괄호를 끼우면 «안 닫힌 줄»로 계속 세어진다. 부가정보는 **콜론 뒤**에. 값 = `#카드번호` · 다른 커밋 해시 · `이 커밋` · `구기록` · `기록만`.
> ★**수리 후보가 여럿이면 «어디까지 넣었는지»를 적고, 안 넣은 것은 새 줄로 분가**시켜라 — 통째로 닫으면 남은 반쪽을 아무도 다시 안 본다.
> ★**적었으면 «마찰 수거» 카드 `#<수거카드번호>` 에 `@<공통개발자>` 멘션 한 줄**(카드가 없는 repo 면 «없음»). 이 파일은 아무 기계도 내용을 안 읽어서, 파일에만 적으면 **아무에게도 안 간다**. **집을 때도 한 줄** — 「이건 제가 집습니다」.
> ★**끝난 줄은 최근 5개만 남기고 보관으로**: `python3 scripts/archive-friction.py docs/friction.md docs/friction-보관.md 5` — **지우지 마라, 옮겨라**(마찰 줄은 규칙의 «영수증»이다).
> **수리 우선순위**: `[막힘]` = 즉시. 같은 것이 **두 번** 나오면 수리 후보. 나머지는 기록만.
> 경위·실측·«왜 이 규약인가» = `friction-규약.md` · 파일 종류별 읽기 처방 = `.claude/rules/verify.md` §4-b

---

(없음)

(없음)

(없음)

(없음)

(없음)

(없음)

(없음)

## 지금 살아 있는 것 — 아직 안 고쳐진 줄
(없음)

## 최근에 끝난 것 (최신 5줄 — 그 앞은 `friction-보관.md`)
2026-09-10 킷동기 — ★★**배포처 `CLAUDE.md` 가 «정확히 200줄»(한도 200)이라 킷이 CLAUDE.md 에 새로 넣은 줄은 그대로는 못 간다.** 어제 `78c9172` 가 «확인 창을 세우려면 전수 표에 줄을 더하라»를 킷 `CLAUDE.md` 에 넣었는데, bnsone 은 **줄 하나 넣을 자리가 없다**(실측 200줄). 기존 줄에 덧붙이기(줄당 300자)나 `rules/` 로 빼기 둘뿐인데, **킷만 CLAUDE.md 에 두고 배포처는 `rules/` 로 빼면 두 곳이 갈린다** — 이 킷이 반복해서 밟은 실패다. → **킷에서도 `rules/verify.md` §6 으로 옮겼다**(그 파일은 `paths:` 가 없어 상시 로드라 «항상 읽히는 층» 요건을 그대로 만족한다). `CLAUDE.md` 는 한 줄 줄었다. ★교훈: **킷 `CLAUDE.md` 에 줄을 더하기 전에 «배포처에 자리가 있나»를 먼저 재라**(`wc -l` 한 번) — 자리가 없으면 처음부터 `rules/` 로 써라. 나중에 옮기면 두 repo 가 갈린 채로 며칠을 간다. [닫힘:이 커밋]
2026-09-10 킷CI — ★★**「차단형 검사기」라고 적어 놓고 어느 CI 잡에서도 안 돌고 있었다.** 어제 만든 `scripts/check-asks.sh`(확인 창 전수 대조)는 `CLAUDE.md`·`_reference/asks.md` 양쪽이 「차단형」이라 말하는데, 킷·bnsone **둘 다 그 스크립트를 부르는 잡이 0건**이었다(`grep -c check-asks .gitlab-ci.yml` = 0). `self-tests` 잡은 `scripts/test-*.sh` 만 줍기 때문에 **검사기 본체는 이름 규칙에서 빠져 자동으로도 안 걸린다.** 즉 표에 없는 창을 훅에 몰래 넣어도 아무도 못 잡는 상태였다 — 이 검사기가 막으려던 바로 그 일이다. ★그리고 **자기시험도 없었다**(다른 검사기 넷은 다 있다). → 고친 것: `ask-check` 잡 신설(차단·`changes: .claude/hooks/**`·표·검사기) + `scripts/test-check-asks.sh` 13케이스. 잣대는 **돌연변이로 증명**했다 — `check-asks.sh` 의 `exit 1` 을 `exit 0` 으로 바꾼 사본에 돌리니 **3 FAIL**(멀쩡한 본체에는 13 OK / 0 FAIL). ★교훈: **«차단형»은 «exit 1 을 쓴다»가 아니라 «그것을 부르는 잡이 있다»다.** 새 검사기를 만들면 그 자리에서 `grep -c <파일명> .gitlab-ci.yml` 을 한 번 쳐라. [닫힘:이 커밋]
2026-09-10 문서구조 — ★★★**오너 「19,900자도 너무 긴거 아니야? … 최신이나 중요한걸 앞단에 놓는건 어떻게 생각해?」 — 둘 다 맞았고, 근거를 공식 문서에서 확인했다.** ⑴**구분기호**: Anthropic 권고 = 지시·자료·예시가 섞이면 **각각을 태그·제목으로 구획하면 오해가 준다**(사람 편의가 아니라 정확도 문제다). 단 같은 문서가 **「모델이 좋아질수록 형식의 중요도는 줄어든다」**고 단서를 달아, **꾸미려고 태그를 두르면 토큰만 먹는다** — 구획은 «어디를 봐야 하나»를 표시할 때만. ⑵**순서는 축이 둘이라 헷갈리면 안 된다**: «긴 자료 위, 질문 아래»는 **한 번의 대화** 안 배치이고(복잡한 다문서 입력에서 질문을 끝에 두면 **최대 30%** 향상이 측정됨), 오너가 말한 «최신을 앞단에»는 **잘릴 수 있는 파일** 안 배치다 — 잘려도 살아남는 쪽이 최신이어야 한다. append 전용이면 정확히 반대로 남는다. ★**그 자리에서 킷 구멍을 하나 찾았다 — 「스킬은 컴팩션 뒤 앞 5,000토큰만 남는다」가 킷 규칙 어디에도 없었다**(`grep '컴팩션'` 0건. 내 메모리와 `check-skill-size.sh` 경고문에만 있었다 — 새 세션은 모른다). → `rules/verify.md` **§4-b 신설**: 파일이 **«어떻게 읽히는가»별로 처방을 갈랐다**(항상 통째 = 주제별 구획·최신순 금지 / 잘린다 = 중요한 것 맨 앞·보조 파일 / 명령으로 잘라 읽는다 = `head` 말고 «잘렸다» 표시 / 쌓인다 = 최신 위·한도·비우는 명령). 그리고 마찰 로그 자체를 그 처방대로 다시 짰다: **머리 규약 3,345자를 `friction-규약.md` 로 빼고**, 본문을 **`## 지금 살아 있는 것` / `## 최근에 끝난 것`** 두 구획으로 나누고, **최신이 맨 위**, 끝난 줄은 **최근 5개만**. 결과 **50,524 → 6,038자**(항목 71 = 작업 5 + 보관 66, 손실 0). ★교훈: **«전부에 같은 처방»이 답이 아니다 — 그 파일이 어떻게 읽히는지가 처방을 정한다.** [닫힘:이 커밋]
2026-09-10 /done — ★★★**오너가 «리뷰가 너무 오래 걸리고 토큰을 많이 쓴다»를 세 번 말했고, 킷은 두 번 다 반대로 옮겼다.** 오너(9-10) = 「done이 xhigh라서 너무 오래걸리고 토큰 많이써. high로 낮추고, 카드 수준에 맞게 돌리도록해, 그래서 러프한 수정이면 러프하게 리뷰하도록 해 … 이 얘기 여러번 한거 같은데 너가 왜 안고칠까?」. 되짚어 보니 ⑴**9-08**: 「AI 가 부사(가볍게·보통)를 low 로 옮긴다」는 실측(70건 중 40건 low·medium)을 근거로 **전 스킬을 xhigh 로 못 박았다** — 문제는 «누가 정하나»였는데 답을 «항상 최고로»로 냈다. ⑵**9-09**: 리뷰 10분을 놓고 오너가 「너무 단순하게 개선하려는 것 같다」고 했는데, 세션이 이를 **「수준을 낮추지 마라」로 읽고** `단계상세.md` §9 에 「⚠️그렇다고 수준을 낮추는 쪽으로 가지 마라」를 새로 박았다. 오너 말뜻은 «손잡이를 수준 하나로 단정하지 마라»였다. → **고친 것**: `/done` frontmatter `xhigh`→`high` · 리뷰 수준을 **카드가 정하게** 했다(`고위험`·`k8s/**`·인증·권한·결제·마이그레이션 → `xhigh` / **`*.md` 만 → `medium`** / 그 밖 → `high`). 판정은 여전히 AI 부사가 아니라 **`git diff --name-only` 한 번**으로 결정된다(9-08 「AI 는 effort 를 판정하지 않는다」와 충돌하지 않는다 — 바뀐 것은 «무엇으로 정하나»다). `merge-review.js` 는 `args.level` 을 받고 **반박표는 한 단 낮춰** 돈다(발견 1건당 3표라 토큰이 가장 많이 드는 자리이고, 반박은 «인용이 그 줄에 실재하나» 확인이라 수준에 덜 민감하다). 표·frontmatter·워크플로 셋을 같은 커밋에서 고쳤다. ★교훈: **같은 요청이 두 번째로 오면 «내가 지난번에 반대로 옮기지 않았나»부터 봐라** — 오너의 부정문(「그렇게 단순하지 않다」)을 «현행 유지»로 읽는 것이 이 킷의 반복 실패다. [닫힘:이 커밋]
2026-09-10 보고서식 — ★★★**«㉢ 알아만 둘 것»이 «안 고치는 서랍»으로 쓰이고 있었다.** 오너 = 「알아만 둘 것이라고 수 많은 세션이 쓰던데 그걸 근본적으로 고치라니까 왜 계속 알아만 두라는거야?」. 바로 앞 보고에서 내가 그 짓을 했다 — `bash -c` 규칙 뒤집기와 ssh 허용 줄 오류를 **이미 고쳐 놓고도** ㉢에 적어 「알아만 두시라」로 냈다. 9-09 엔 다른 세션이 «리뷰 범위 좁히기»를 ㉢에 넣고 닫았다. 3구획은 «사람이 움직일 것»을 가리는 장치인데, ㉢이 **AI 가 자기 몫을 사람 눈앞에 쌓아 두는 자리**가 됐다. → CLAUDE.md 3구획 규칙과 `/todo` 보고 서식에 한 물음을 박았다: **㉢에 한 줄 적기 전에 「이걸 지금 내가 고칠 수 있나?」 — 고칠 수 있으면 ㉢ 금지, 그 자리에서 고치고 «고쳤다»로 적어라.** ㉢에 남는 것은 남의 저장소·사람 권한·아직 못 잰 값 정도다. ★교훈: **구획 이름이 형용사면(«알아만 둘») 미루기가 그리로 흘러든다 — 확인 가능한 물음("내가 고칠 수 있나")으로 문을 좁혀라.** [닫힘:이 커밋]
