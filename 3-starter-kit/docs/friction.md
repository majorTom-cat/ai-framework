# 마찰 로그 — ★최신이 맨 위

> **적는 법**: `YYYY-MM-DD /스킬 — 무엇이 (한 줄)` 을 **아래 «지금 살아 있는 것» 맨 위**에 넣는다(끝에 붙이지 마라). 막힘·우회는 줄 머리에 **`[막힘]`**. 스킬 마무리 때 그 커밋에 동봉 — 별도 커밋·카드 불요.
> **닫는 법**: 고친 커밋이 그 줄 끝에 **`[닫힘:…]`** 을 붙인다. ⚠️기계가 보는 건 **`[닫힘:` 접두어뿐** — `[닫힘(①만):` 처럼 괄호를 끼우면 «안 닫힌 줄»로 계속 세어진다. 부가정보는 **콜론 뒤**에. 값 = `#카드번호` · 다른 커밋 해시 · `이 커밋` · `구기록` · `기록만`.
> ★**수리 후보가 여럿이면 «어디까지 넣었는지»를 적고, 안 넣은 것은 새 줄로 분가**시켜라 — 통째로 닫으면 남은 반쪽을 아무도 다시 안 본다.
> ★**적었으면 «마찰 수거» 카드 `#<수거카드번호>` 에 `@<공통개발자>` 멘션 한 줄**(카드가 없는 repo 면 «없음»). 이 파일은 아무 기계도 내용을 안 읽어서, 파일에만 적으면 **아무에게도 안 간다**. **집을 때도 한 줄** — 「이건 제가 집습니다」.
> ★**끝난 줄은 최근 5개만 남기고 보관으로**: `python3 scripts/archive-friction.py docs/friction.md docs/friction-보관.md 5` — **지우지 마라, 옮겨라**(마찰 줄은 규칙의 «영수증»이다).
> **수리 우선순위**: `[막힘]` = 즉시. 같은 것이 **두 번** 나오면 수리 후보. 나머지는 기록만.
> 경위·실측·«왜 이 규약인가» = `friction-규약.md` · 파일 종류별 읽기 처방 = `.claude/rules/verify.md` §4-b

---

## 지금 살아 있는 것 — 아직 안 고쳐진 줄
(없음)

## 최근에 끝난 것 (최신 5줄 — 그 앞은 `friction-보관.md`)
2026-09-16 /todo — ★★**어제 넣은 CI 검사기가 «repo 루트가 아닌 곳»에서 돌면 두 방향 모두 거짓말한다 — 킷 저장소에서 내가 그 초록을 받았다.** 문서 수리 뒤 확인차 `3-starter-kit/` 안에서 `check-append-only-git.sh` 를 돌렸더니 **friction 두 파일 모두 «신규»로 건너뛰고 「사라진 항목 없음 — OK」**(검사 **0건**). 원인은 `PAIRS` 가 **repo 루트 기준 경로**라 `-f` 로는 «보이는데» `git show <ref>:<경로>` 만 빗나간 것 — 파일이 없는 게 아니라 **잣대가 다른 자리를 가리켰다**. ⚠️거짓 초록만이 아니다: 픽스처의 하위 폴더에서 돌리면 같은 이유로 **거짓 ⛔**(「누적 파일이 통째로 사라졌다」·exit 1)도 난다 — **한 결함이 놓침과 오탐을 동시에 낸다.** ⇒ 맨 앞에 `git rev-parse --show-prefix` 가름을 세워 **판정 불능 `exit 2`** + 안내 「①루트로 옮겨 다시 돌려라 ②킷처럼 하위 폴더에 놓인 곳이면 여기서 돌리는 게 아니다 — 이 파일은 «배포처 CI» 전용이고 사람은 `check-append-only.py` 에 경로를 직접 준다」. 자기시험 케이스 `L` 추가(**12 OK / 0 FAIL**) · ★**돌연변이로 증명 — 가름을 빼니 `L1`(exit 2 기대 → 실제 1)·`L2` 가 FAIL**(11 OK / 2 FAIL). ★교훈: **«검사 0건»은 `F` 케이스(대상 파일이 아예 없는 스택 = 진짜 통과)와 겉모양이 같다 — «0건이 나올 수 있는 다른 이유»를 다 세지 않으면 그중 하나는 조용한 초록이 된다.** 어제 같은 파일에서 «없다»를 넷으로 갈랐는데 **«어디서 돌았나»라는 다섯째를 안 셌다.** [닫힘:이 커밋]
2026-09-16 /todo — ★★**킷 문서가 «나중에 볼 후보»로 적어 둔 것을 오너가 «지금 쓰고 있는 것»으로 읽었다.** 오너 질문(「bnsone 이 gitlab 이랑 연동해서 카드도 만드는데 이게 mcp 인가?」)에서 드러났다. 실제 경로는 `glab` 명령이다 [잣대: `card` 스킬 안 `glab issue …` **5곳** · 이 맥에 꽂힌 MCP 는 playwright·context7 **둘뿐**(`claude mcp list`) · 세션 기록 전체에서 `mcp__gitlab` **실호출 0건**(잡힌 2건은 조사 문서 본문이었다)]. 「gitlab MCP」는 README '도입 후보' 표와 리더 가이드 **P2 줄에만** 있었다 — **«후보»가 «미도입»을 뜻한다는 것은 그 표를 만든 사람에게만 자명하다.** ⇒ ⑴P2 줄에 「**«후보»는 «지금 쓰는 것»이 아니다 — 킷은 이 셋 없이 돈다**」를 명시(킷판 + `2-team-dev` 사본 **둘 다**) ⑵조건을 **확인 가능한 물음**으로 바꿨다 — 공식 MCP 는 **18.6+ · Premium/Ultimate · Duo** 필요이므로 **`glab api version` 의 `version`·`enterprise` 를 먼저 재라**(2026-09-16 실측 사내 = **`16.4.1` · `enterprise:false`** = **두 칸 다 미달·불가**) ⑶조사 값 갱신 — 커뮤니티판 도구 **170 → 261개**, 고르는 손잡이(`GITLAB_TOOLSETS`·`GITLAB_PERMISSION_MODE`), MCP 가 CLI 대비 토큰 **4~32배**라는 공개 측정, **사내 PAT 를 외부 npm 패키지에 넘긴다**는 고지 대상(context7 행과 같은 결), 그리고 **갈아타는 비용**(킷 스킬 **19개**·`glab` 호출 **141곳**). ⚠️커뮤니티판은 **받쳐 주는 최소 GitLab 버전 표가 없어** 구버전에서 되는지는 **찾아봐서는 안 나온다** — 붙여 봐야 안다. ★교훈: **«후보·보류·검토중» 딱지는 그 목록을 만든 사람에게만 «미도입»으로 읽힌다 — 읽는 사람이 «지금 이게 돌고 있나»를 그 문서 안에서 답할 수 있게 적어라.** [닫힘:이 커밋]
2026-09-16 /todo — ★★**같은 검사를 따로 만든 두 팀이 «서로의 빈자리»를 하루에 네 번 메웠다 — 마지막 것은 킷에만 남아 있던 조용한 구멍이었다.** 배포처(`#285`)가 자기 리뷰 4회에서 잡은 값 셋을 보내 와 킷에서 전부 재봤다: ①대상 파일이 «기준에도 트리에도» 없을 때 무조건 ⛔ → **킷은 이미 merge base 로 갈랐다**(케이스 `I`·`J`) ②자기시험이 `CI_MERGE_REQUEST_TARGET_BRANCH_NAME` 을 물려받아 깨짐 → **킷 판은 환경변수를 안 읽는다**(그 변수를 넣고 돌려 `EXIT=0` 확인) ③**tip 에 대상 파일이 없을 때 조용히 통과** → **이건 그대로 있었다.** 재현: main 이 보관 파일을 지운 뒤, 내 가지가 그 파일 «안에서 한 줄을 지웠는데» 「신규」로 읽혀 **검사가 한 번도 안 돌고 exit 0**. ⇒ 같은 잣대(merge base)로 «내가 처음 만든 것»과 «대상 브랜치가 지운 것»을 갈라 뒤쪽을 **판정 불능 `exit 2`** 로 세웠다(케이스 `K` · 돌연변이로 가름을 빼니 `K1`~`K3` 셋이 FAIL). ★**안내 «순서»까지 시험에 넣었다**(`K3`) — 「①최신본을 얹어라 →②그래도면 목록을 고쳐라」를 뒤집으면 **사람이 목록에서 그 파일을 빼 게이트를 스스로 끈다**(배포처가 재현한 경로다). ⚠️이 파일(`check-append-only-git.sh`)은 **bnsone 에 안 보낸다** — 그쪽은 `#285` 의 `check-append-only-ci.sh` 배선을 쓴다(잡 이름이 같아 두 벌이면 충돌). ★교훈: **«없다»는 한 낱말에 서로 다른 사건이 몇 개 숨어 있나를 세라** — 이 검사기 한 곳에서만 «없다»가 네 가지였고(신규·낡은 브랜치·내가 지움·상대가 지움), 넷을 안 가르면 그중 둘은 반드시 틀린 판정을 받는다. [닫힘:이 커밋]
2026-09-16 /todo — ★★**오늘 만든 CI 검사가 «정상 작업을 막고, 진짜 사고는 놓치는» 구멍을 둘 다 갖고 있었다 — 같은 검사를 만들던 배포처(`#285`)가 먼저 밟고 알려 왔다.** ⑴**거짓 ⛔**: `archive-friction.py` 는 보관할 때 «글자까지 같은» 중복 줄을 접는데(그 파일 `seen`/`merged`), 검사기가 그걸 «사라짐»으로 읽어 **보관 작업 MR 이 통째로 막힌다**(킷에서 재현 — exit 1). ⇒ `check-append-only.py` 가 **기준·새 판·보관 세 쪽 모두** 완전 동일 줄을 접게 했다(`entries()` 한 곳에서만). ★**«한쪽만» 접으면 안 접느니만 못하다** — 남은 여분이 «진짜 사라진 다른 줄»의 짝 노릇을 해 **삭제를 가린다**(돌연변이로 재현: 「사라지는 항목 없음 — 올려도 된다」가 찍혔다). 케이스 `G`·`H` 로 고정. ⑵**놓침**: 대상 파일이 트리에 없으면 무조건 건너뛰어 **누적 파일을 통째로 지운 MR 이 통과**했다. 그런데 그 «없음»은 두 가지다 — ⓐ그 파일이 생기기 «전»에서 갈라진 낡은 브랜치(배포처에 실제로 2개 살아 있다 = 통과해야 한다) ⓑ내가 지웠다(막아야 한다). **갈라진 지점(merge base)에 그 파일이 있었나**로 갈랐다. 케이스 `I`·`J` 가 «같은 모양, 반대 판정»으로 서로를 붙든다. ★돌연변이 3종 전부 기대대로 FAIL(접기 제거→`G` · 한쪽만 접기→`H` · 가름 제거→`J`). ★**덧붙임 — 짝짓기 «경로»가 셋이라 시험도 경로마다 있어야 한다**: 처음 넣은 `H` 는 줄이 80자보다 짧아 ③(같은 날짜+닮음 0.7↑)으로만 잡히고 있었고, ②(앞 80자로 시작)는 비어 있었다. 배포처는 **정반대로** ②만 덮고 ③이 비어 있었다 — 서로 알려 줘 양쪽이 채웠다(킷 `I` 추가 · 한쪽만 접기 돌연변이에서 `H`·`I` 가 **함께** FAIL). ★교훈: **«통과시켜야 하는 모양»과 «막아야 하는 모양»이 겉으로 같으면, 둘을 가르는 «세 번째 사실»을 찾아라** — 못 찾은 채 한쪽으로 몰면 둘 중 하나는 반드시 틀린다. 그리고 **검사에 «경로»가 여럿이면 한 경로의 초록이 옆 경로를 보증하지 않는다.** 이번 넷 다 상대 팀이 먼저 밟았다 — **같은 것을 만드는 다른 팀이 있으면 결함도 둘이 나눠 갖는다.** [닫힘:이 커밋]
2026-09-16 /handoff — ★★**어제 넣은 규칙 한 줄이, 그 규칙을 실행하는 스킬과 «정반대»를 말하고 있었다 — 둘 다 성실히 따르면 모순이다.** 배포처 세션(`bnsone-30`)이 두 건을 짚어 왔고 킷에서 다시 쟀다. ⑴`CLAUDE.md` 가 「개인 메모리는 «내 PC·내 계정» 사정만」이라 적었는데, **그 범주는 `/handoff` 5단계 ②가 이미 `CLAUDE.local.md` 로 보내는 것**이고 개인 메모리는 ③«다음 프로젝트에서도 반복될 것»이다 — **한 범주에 행선지가 둘.** ⇒ 「내 포트·경로는 `CLAUDE.local.md`」를 명시하고 개인 메모리를 ③ 정의로 맞췄다(+판정 주체를 «`/handoff` 5단계»로 가리킴). ⑵그 근거 숫자가 **해석이었다** — 실제로 잰 것은 «메모 타입»(내 맥에서 재확인: `feedback` 128 · `project` 9 · `reference` 5 · `user` **0** = 142)인데 스킬엔 「128건이 팀 규칙 자리에 있어야 할 것」으로 적혀 있었다. **잰 것만** 남겼다(킷 정본 + 루트 사본 2곳 · `AI-CONTEXT.md` 는 해당 줄 없음을 grep 으로 확인). ★교훈 둘: **규칙을 «항상 읽히는 층»에 새로 넣을 땐 그것을 실행하는 스킬의 분류와 나란히 놓고 읽어라** — 요약하면서 범주가 어긋나고, 그 어긋남은 둘 다 읽는 세션에서만 드러난다. 그리고 **남이 준 숫자는 «잰 것»과 «해석»을 갈라서 인용하라** — 해석이 정본에 박히면 다음 세션은 그것을 실측으로 인용한다. ⑶**같은 날 덧붙임 — 같은 줄의 «색인↔본문»도 어긋나 있었다**: 색인은 「절차가 막혔으면」인데 **바로 다음 줄** 본문은 「막혔거나·우회했거나·매끄럽지 않았으면」이다. 색인이 더 좁으면 **우회했을 뿐·좀 불편했을 뿐인 마찰이 «대상 아님»으로 걸러진다**(배포처 fresh-context 리뷰가 [Low]로 잡은 것을 그 세션이 전해 왔다). 본문 조건에 맞췄다 — 여유를 내려고 「다른 사람」을 「동료」로 줄여 **295/300자**. ★교훈 셋: **색인 줄은 본문 조건을 «줄여 쓰면» 안 된다** — 줄이는 순간 본문이 잡던 것에 구멍이 나고, 색인만 읽는 세션은 그 구멍을 못 본다. [닫힘:이 커밋]
