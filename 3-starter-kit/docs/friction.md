# 마찰 로그 — ★최신이 맨 위

> **적는 법**: `YYYY-MM-DD /스킬 — 무엇이 (한 줄)` 을 **아래 «지금 살아 있는 것» 맨 위**에 넣는다(끝에 붙이지 마라). 막힘·우회는 줄 머리에 **`[막힘]`**. 스킬 마무리 때 그 커밋에 동봉 — 별도 커밋·카드 불요.
> **닫는 법**: 고친 커밋이 그 줄 끝에 **`[닫힘:…]`** 을 붙인다. ⚠️기계가 보는 건 **`[닫힘:` 접두어뿐** — `[닫힘(①만):` 처럼 괄호를 끼우면 «안 닫힌 줄»로 계속 세어진다. 부가정보는 **콜론 뒤**에. 값 = `#카드번호` · 다른 커밋 해시 · `이 커밋` · `구기록` · `기록만`.
> ★**수리 후보가 여럿이면 «어디까지 넣었는지»를 적고, 안 넣은 것은 새 줄로 분가**시켜라 — 통째로 닫으면 남은 반쪽을 아무도 다시 안 본다.
> ★**적었으면 «마찰 수거» 카드 `#<수거카드번호>` 에 `@<공통개발자>` 멘션 한 줄**(카드가 없는 repo 면 «없음»). 이 파일은 아무 기계도 내용을 안 읽어서, 파일에만 적으면 **아무에게도 안 간다**. **집을 때도 한 줄** — 「이건 제가 집습니다」.
> ★**끝난 줄은 최근 5개만 남기고 보관으로**: `python3 scripts/archive-friction.py docs/friction.md docs/friction-보관.md 5` — **지우지 마라, 옮겨라**(마찰 줄은 규칙의 «영수증»이다).
> **수리 우선순위**: `[막힘]` = 즉시. 같은 것이 **두 번** 나오면 수리 후보. 나머지는 기록만.
> 경위·실측·«왜 이 규약인가» = `friction-규약.md` · 파일 종류별 읽기 처방 = `.claude/rules/verify.md` §4-b

---

## 지금 살아 있는 것 — 아직 안 고쳐진 줄
(없음)

## 최근에 끝난 것 (최신 5줄 — 그 앞은 `friction-보관.md`)
2026-09-23 세션 훅·/dev·/done·/adr — ★★**«따라잡았다»가 끝이 아니었다 — 같은 자리에서 두 번째**(bnsone-85 발신 · 팀원 simon2001 제보 · #306 · 원 줄 #322). 9-21 에 넣은 경고는 «ORM 재생성»만 시켰는데, 남의 마이그레이션을 따라잡으면 **내 dev DB 에도 적용**해야 한다 — 안 하면 재시드·타입검사가 «그런 표가 없다»(P2021)로 죽고 **코드 결함처럼 보인다**(배포처는 `migrate deploy` 10건 + 재시드로 풀었다). ⇒ 훅 문구를 ①생성 ②dev DB 적용(컨테이너가 자동 적용하면 생략) ③서버 재시작 셋으로 넓히고, `/dev` 단계상세 §2 에 «생성 ≠ 적용»을 박았다. 자기시험 L5 가 이제 두 조각(`스키마 변경`·`migrate deploy`)을 다 본다(80 OK). ★교훈: **«무엇이 낡았나»를 한 겹만 세면 다음 겹에서 같은 사고가 난다** — 생성물·DB·떠 있는 프로세스는 각각 따로 낡는다. ★★**ADR 번호가 병행 브랜치끼리 겹쳤다**(#311 — 0076 을 로컬에서 잡아 둔 사이 다른 카드가 main 에 먼저 머지 → 파일명 + 인용 **15곳** 이동, 그중엔 아직 머지 전이라 고칠 수 있었던 마이그레이션 SQL 주석도 있었다). 채번(`/adr` 5단계)은 **파일이 있는 것만** 보므로 push 안 한 번호는 원리상 못 본다 — 그래서 ㉠ 대신 **㉡·㉢로 막았다**: `/done` 단계상세 §0 에 병합 뒤 중복 점검 한 줄(`ls docs/adr | cut -d- -f1 | sort | uniq -d` 가 비어야 진행) · `rules/migrations.md`+`/adr` 에 «불변 파일에는 ADR 번호 말고 **카드 번호**»(머지되면 그 주석은 영영 못 고친다). [닫힘:이 커밋]
2026-09-21 여러 스킬 — ★★**배포처가 모아 보낸 안 닫힌 마찰 15건(A~O)을 킷에 반영했다**(bnsone-a8 발신 · 그쪽이 origin/main 대조로 «전부 미반영» 확인). ★**A 가 가장 컸다**: `check-append-only.py` 가 git ref 를 받으면 «기준 0항목 · EXIT 0»으로 **조용히 통과**했다(실패의 모양 = 성공의 모양). ⇒ 세 인자 모두 실재 파일이 아니면 **exit 2** + `CLAUDE.md`·`_reference/append-only.md` 문구를 «뽑아 둔 파일»로 · 자기시험 J 4케이스(13 OK). 나머지: B `/inspect` 요소는 이름으로 집어라(종류만으로 집으면 폼이 하나 더 서는 순간 다른 폼을 시험한다) · C `CLAUDE.md` 실행 절 «로컬 테스트는 CI 의 일부다» · D `/dev` §2 스키마 바뀐 카드는 생성 뒤 **떠 있는 서버도 재시작**(빈 Suspense 로 보인다) · E `/docs` `상세.md` §4 + `verify.md` §2 «절 번호가 밀리면 인용을 두 벌 잣대·변형까지»(29곳으로 세고 고쳤는데 실제 37곳) · F `_reference/worktree.md` 미추적 파일은 워크트리를 안 따라온다 · G `/adr` ADR 여러 장은 카드별 커밋 · H 질문 묶음이 **소제목을 버리던 것**을 고침(스크립트 + `rules/docs.md` 「불릿은 홀로 읽혀야 한다」 + 시험) · I `/done` §9 **리뷰가 도는 동안 HEAD 를 건드리지 마라** · J `merge-review` args 는 객체로(문자열이면 한글이 깨져 0턴) · K **세션 시작 훅**: 따라온 커밋에 스키마가 섞였으면 «생성 다시» 한 줄(자기시험 L5·L5b, **옛 훅으로 돌려 FAIL 남을 확인**) · L `/inspect` 함정 §1 로그인 뒤 **최종 URL** 부터(로그인 실패도 200 — 거짓 실패 15/24) + 파이썬 쿠키 파서가 `#HttpOnly_` 를 버린다 · M `/dev` §3 착수 전 그 절의 «미정·후속» 훑기 · N `/done` 함정 §4 «브랜치 소멸»만 보지 말고 판정기로 · O `verify.md` §2 전수 조회는 `grep -a`(NUL 든 파일을 맥 grep 이 조용히 건너뛴다). ⚠️**킷에 자리가 없어 안 넣은 것 셋**: I 의 `startsWith` 대조는 배포처 게이트 스크립트 몫 · C 의 잡 이름(`check-failclosed` 류)은 그 repo 고유라 일반 문장으로 · M 의 `/card` 2단계는 본문이 한도(5,372자)라 `/done` §2 ㉮ 가 이미 덮는 것으로 갈음. 자기시험 12개 전부 초록(78→80) · 스킬 한도·밀도·확인 창 OK. [닫힘:이 커밋]
2026-09-17 /done·/docs·/adr·/overhaul — ★★★**1차 요구 전수 대조가 어떤 절차에도 없어서, 카드 하나하나는 초록인데 «요구 전체에서 빠진 것»이 쌓였다**(bnsone-74 제보 · 오너 확인 후 발송 · 오너 「다 해」). 구멍 6종 → ①**번호 없는 미룸**(예고 카드 11·분해안 5·범위 밖 1차 기능 3 추적 0) ⇒ 새 검사기 `check-deferred.sh`(미룸 문구 뒤를 ` · `/`;` 로 나눠 **조각마다** `#번호` 또는 `(카드 불요: 사유)` — 한 줄에 카드 하나만 달고 나머지를 흘리는 실사고 모양) + 차단형 CI `deferred-check`(MR 이 더한 줄만 · `--all` 은 주기 점검) + `/done` 단계상세 §2 ㉮ + `/adr` 5단계. bnsone 실물 한 바퀴 = 옛 부채 36줄(첫 잣대는 맨 «범위 밖»이 업무 용어와 겹쳐 49줄 → 좁힘). ②**스펙 이탈이 카드 댓글에만** ⇒ `/done` §2 ㉯ «달라진 점 없음»은 카드 댓글의 결정 흔적 ↔ digest 대조 후에만. ③**새 기획 문서가 ADR 을 뒤집어도 모름** ⇒ `/docs` `상세.md` §1(ADR·기술설계 대조, 새 보조 파일). ④**원문 보호 뚫림** — 실측 결과 두 커밋 모두 가드 생기기 «전»(bnsone-74 가 9-16 이후 0건·시험 20/20 확인) → 킷 수정 없음(`planner-docs` 이관은 따로). ⑤**기획자는 카드를 안 본다** ⇒ 질문 정본 = digest «미해결·질문» + `collect-planner-questions.sh` 묶음을 오너가 전달(`rules/docs.md`·`CLAUDE.md` 177행). ⑥**digest 가 풀린 질문을 «미정»으로 든다** ⇒ 같은 스크립트 `--stale`(식별자 경계 일치 — `C-1` 이 `C-13` 에 걸리던 첫 판을 고침 · bnsone 후보 23줄 · 식별자 없는 24건은 못 본다고 찍음). ⑦공통 원인 ⇒ `/overhaul` ④요구 갈래(`요구.md`). 자기시험 2개 추가(16·13 OK) · **돌연변이 3종으로 증명**(줄 단위 판정 → C 2 FAIL · 경계 없는 찾기 → D 1 FAIL · 해소 표시 무시 → B 4 FAIL) · 킷 자기시험 12개 전부 초록 · 스킬 한도·밀도 OK. ⚠️`/docs`·`/done` 본문은 한도에 차 있어 포인터만. 짝(규칙 1건 추가 = 삭제 후보): CLAUDE.md 는 기존 줄 보강이라 줄 수 그대로. [닫힘:이 커밋]
2026-09-17 /todo·/adr — ★**스킬 문구가 실제 동작과 달랐던 두 자리**(bnsone-4c 실측 제보). ①`/todo` 0-d «ID 나열도 20건 안팎씩 나눠» — `glab todo done` 은 ID 를 **한 호출에 하나만** 받는다(`Accepts at most 1 arg(s), received 3.` EXIT 1). ⇒ «한 호출에 하나 · 병렬 · 재조회»로(본문은 한도 안에서 짧게, 경위는 `스캔상세.md` §3 — 9-02 «첫 건만 실패» 사고도 같은 원인으로 보인다고 적음). 방금 넣은 `eea24dc` 판에도 옛 줄이 남아 있었다. ②`/adr` 5단계 — 인계문에 «0061 로 바꾼다»만 적은 사이 다른 세션이 원격 브랜치 전수 조회대로 0061 을 썼다. ⇒ «예약은 파일로만 — 그 자리에서 파일을 만들어 브랜치로 push». 제보의 «INDEX 예약 줄» 대안은 **안 넣었다** — 채번 조회가 파일명만 모으므로 INDEX 줄은 못 본다. [닫힘:이 커밋]
2026-09-17 /todo — ★**0-d «끝난 To-Do 비우기»가 닫힌 항목까지 사용자에게 묻고 있었다 — «아니오»가 나올 수 없는 질문이다.** bnsone 에서 닫힌 #249 의 To-Do 한 건이 세 세션 연속 질문에 올라 답 없이 쌓였고, 오너가 「왜 매번 손으로 지워야 하나」 → 확인 없이 지우고 한 줄 보고로 결정(bnsone-4c 발신 · 킷 세션에서 오너 「1」로 재확인). ⇒ `todo/SKILL.md` 0-d 본문·계약 줄 + `스캔상세.md` §3. 안전장치(`state != "opened"` 필터 · `--all` 금지 · 20건씩 · 재조회)는 그대로. 스킬이 한도에 차 있어 본문엔 짧은 줄만 두고 경위는 §3 으로(5,376자 이내 확인). ⚠️자동 모드 판정기가 이 편집을 «자기 수정»으로 두 번 막았다 — 오너가 모드를 바꾼 뒤 통과. [닫힘:이 커밋]
