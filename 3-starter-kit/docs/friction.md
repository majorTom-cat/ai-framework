# 마찰 로그 — ★최신이 맨 위

> **적는 법**: `YYYY-MM-DD /스킬 — 무엇이 (한 줄)` 을 **아래 «지금 살아 있는 것» 맨 위**에 넣는다(끝에 붙이지 마라). 막힘·우회는 줄 머리에 **`[막힘]`**. 스킬 마무리 때 그 커밋에 동봉 — 별도 커밋·카드 불요.
> **닫는 법**: 고친 커밋이 그 줄 끝에 **`[닫힘:…]`** 을 붙인다. ⚠️기계가 보는 건 **`[닫힘:` 접두어뿐** — `[닫힘(①만):` 처럼 괄호를 끼우면 «안 닫힌 줄»로 계속 세어진다. 부가정보는 **콜론 뒤**에. 값 = `#카드번호` · 다른 커밋 해시 · `이 커밋` · `구기록` · `기록만`.
> ★**수리 후보가 여럿이면 «어디까지 넣었는지»를 적고, 안 넣은 것은 새 줄로 분가**시켜라 — 통째로 닫으면 남은 반쪽을 아무도 다시 안 본다.
> ★**적었으면 «마찰 수거» 카드 `#<수거카드번호>` 에 `@<공통개발자>` 멘션 한 줄**(카드가 없는 repo 면 «없음»). 이 파일은 아무 기계도 내용을 안 읽어서, 파일에만 적으면 **아무에게도 안 간다**. **집을 때도 한 줄** — 「이건 제가 집습니다」.
> ★**끝난 줄은 최근 5개만 남기고 보관으로**: `python3 scripts/archive-friction.py docs/friction.md docs/friction-보관.md 5` — **지우지 마라, 옮겨라**(마찰 줄은 규칙의 «영수증»이다).
> **수리 우선순위**: `[막힘]` = 즉시. 같은 것이 **두 번** 나오면 수리 후보. 나머지는 기록만.
> 경위·실측·«왜 이 규약인가» = `friction-규약.md` · 파일 종류별 읽기 처방 = `.claude/rules/verify.md` §4-b

---

(없음)

(없음)

(없음)

(없음)

(없음)

(없음)

(없음)

(없음)

(없음)

(없음)

## 지금 살아 있는 것 — 아직 안 고쳐진 줄
(없음)

## 최근에 끝난 것 (최신 5줄 — 그 앞은 `friction-보관.md`)
2026-09-10 확인창 — ★★★**「전수 표」가 전수가 아니었다 — 창은 `.claude/hooks/` 밖에도 있다.** 오너가 「automode 인데 또 떴다」며 창을 찍어 보냈다. 재보니 **원인이 둘로 갈렸다.** ⑴**그 창 자체는 옳았다** — 명령을 옛 `session-guard.sh` 에 그대로 재생하니 「이 클론을 다른 세션이 쓰는 중인데 HEAD를 옮기려 한다(점유 pid 60691)」였고, **점유는 사실이었다.** 킷이 일부러 세운 창이다(2026-08-13 실사고). ★내가 여기서 한 번 헛짚었다 — 먼저 찾은 훅을 원인으로 «단정»해 오너에게 보고했는데, 그 훅의 조건(`*"git checkout"*`)은 사진 속 `git -C <경로> checkout` 형태에 **문자열이 안 맞아 애초에 안 걸린다.** 시험을 돌리고서야 알았다. ⑵**그래도 진짜 결함을 하나 팠다**: bnsone 작업 폴더 `settings.local.json` 에 **인라인 훅**이 하나 숨어 있었고, `git status --porcelain` 으로 물어 **추적 안 하는 파일(`?? .agents/`)까지 세어** 그 명령이 건드리지도 않는 파일을 이유로 창을 세웠다. → 기준을 `git diff`(추적 중인 변경만)로, 대상을 «정말 되돌리는 형태»로 좁혔다. 옛 훅에 같은 시험 **3 FAIL** / 새 훅 **9 OK 0 FAIL**. ★★그리고 **어제 만든 `check-asks.sh` 는 그 창을 한 개도 못 봤다** — `.claude/hooks/` 폴더만 세기 때문이다. 설정 파일 안까지 보도록 넓혔다(발견 시 exit 1). ★교훈 둘: **«전수»라고 적기 전에 «그것이 있을 수 있는 자리»를 세라**(폴더 하나만 센 것이 전수가 아니다) · **창이 떴다고 다 고장이 아니다 — 먼저 그 명령을 훅에 재생해 «누가 세운 창인지»부터 확정하라.** 재생 없이 단정하면 멀쩡한 방어를 뜯는다. [닫힘:이 커밋]
2026-09-10 킷CI — ★★**새 자기시험이 배포처 CI 에서 통째로 떨어졌는데, 로그 어디에도 «왜»가 없었다.** `test-archive-friction.sh` 는 로컬에서 10 OK 였는데 bnsone `self-tests` 잡에서 **7케이스가 전부 «기대와 다름»** 으로 빨개졌다. 원인은 검사기가 아니라 **그 잡 이미지(node:24-slim)에 `python3` 이 없다**는 것이었다(도구가 `.py` 다). ★그런데 **내 시험이 그 이유를 감췄다** — `ck()` 가 «기대/실제»만 찍고 **도구가 뭐라고 했는지는 안 찍었다.** 「검사기가 틀렸다」로 오독하기 딱 좋다. 이 repo 가 반복해 밟은 «의존 명령 부재 = 무음 = 초록»의 변종인데, 이번엔 무음이 아니라 **엉뚱한 범인**을 가리켰다. → 고친 것 셋: ⑴시험 머리에서 `command -v python3` 로 **이름을 대고 죽는다**(부재는 «건너뜀»이 아니라 실패) ⑵실패 케이스마다 **도구 출력 4줄을 함께 찍는다** ⑶킷·배포처 `self-tests` 잡 이미지에 `python3` 을 깐다. 잣대는 shim PATH(python3 만 뺀 최소 PATH)로 증명했다 — 정확히 그 문구로 exit 1. ★교훈: **자기시험은 «틀렸다»와 «못 돌았다»를 갈라 말해야 한다** — 안 가르면 다음 사람이 멀쩡한 검사기를 뜯는다. [닫힘:이 커밋]
2026-09-10 마찰정리 — ★★**보관 도구의 «뒤집을까» 판정이 배포처 실물에서 조용히 틀렸다 — 결과만 보면 정상처럼 보인다.** `archive-friction.py` 는 「`entries[0]` 날짜 < `entries[-1]` 날짜면 옛 순서」로 추측했는데, bnsone `friction.md` 는 25줄이 **첫 줄도 마지막 줄도 2026-09-09** 라 «최신이 위»로 오판했다. 그대로 돌렸으면 **가장 오래된 닫힘 5줄이 남고 최신 닫힘이 보관으로** 갔다 — 하려던 것의 정반대인데 **항목 수는 맞아서 「손실 0」 초록이 그대로 뜬다.** 고치려고 «이웃 쌍의 오름/내림 수 비교»로 바꿨더니 **동점(1:1)에서 같은 오판**이 났다(자기시험이 그 자리에서 잡았다). → **추측을 버렸다: 기본은 파일 순서가 정본이고, 옛 순서 파일은 부르는 쪽이 `--oldest-first` 로 말한다.** 자동 판정은 «날짜가 올라가는 이웃 쌍이 하나라도 있나»(≠«많나»)로 바꿔 **경고만** 낸다. `scripts/test-archive-friction.sh` 신설 10케이스 · 돌연변이(뒤집기 무력화)에 2 FAIL 로 잣대 증명. ★교훈: **«추측하는 판정»은 동점·동값에서 조용히 틀린다 — 부르는 쪽이 말하게 하고, 자동 판정은 경고까지만 해라.** 그리고 **«손실 0» 같은 총량 검사는 «순서가 뒤집혔다»를 절대 못 잡는다.** [닫힘:이 커밋]
2026-09-10 킷동기 — ★★**배포처 `CLAUDE.md` 가 «정확히 200줄»(한도 200)이라 킷이 CLAUDE.md 에 새로 넣은 줄은 그대로는 못 간다.** 어제 `78c9172` 가 «확인 창을 세우려면 전수 표에 줄을 더하라»를 킷 `CLAUDE.md` 에 넣었는데, bnsone 은 **줄 하나 넣을 자리가 없다**(실측 200줄). 기존 줄에 덧붙이기(줄당 300자)나 `rules/` 로 빼기 둘뿐인데, **킷만 CLAUDE.md 에 두고 배포처는 `rules/` 로 빼면 두 곳이 갈린다** — 이 킷이 반복해서 밟은 실패다. → **킷에서도 `rules/verify.md` §6 으로 옮겼다**(그 파일은 `paths:` 가 없어 상시 로드라 «항상 읽히는 층» 요건을 그대로 만족한다). `CLAUDE.md` 는 한 줄 줄었다. ★교훈: **킷 `CLAUDE.md` 에 줄을 더하기 전에 «배포처에 자리가 있나»를 먼저 재라**(`wc -l` 한 번) — 자리가 없으면 처음부터 `rules/` 로 써라. 나중에 옮기면 두 repo 가 갈린 채로 며칠을 간다. [닫힘:이 커밋]
2026-09-10 킷CI — ★★**「차단형 검사기」라고 적어 놓고 어느 CI 잡에서도 안 돌고 있었다.** 어제 만든 `scripts/check-asks.sh`(확인 창 전수 대조)는 `CLAUDE.md`·`_reference/asks.md` 양쪽이 「차단형」이라 말하는데, 킷·bnsone **둘 다 그 스크립트를 부르는 잡이 0건**이었다(`grep -c check-asks .gitlab-ci.yml` = 0). `self-tests` 잡은 `scripts/test-*.sh` 만 줍기 때문에 **검사기 본체는 이름 규칙에서 빠져 자동으로도 안 걸린다.** 즉 표에 없는 창을 훅에 몰래 넣어도 아무도 못 잡는 상태였다 — 이 검사기가 막으려던 바로 그 일이다. ★그리고 **자기시험도 없었다**(다른 검사기 넷은 다 있다). → 고친 것: `ask-check` 잡 신설(차단·`changes: .claude/hooks/**`·표·검사기) + `scripts/test-check-asks.sh` 13케이스. 잣대는 **돌연변이로 증명**했다 — `check-asks.sh` 의 `exit 1` 을 `exit 0` 으로 바꾼 사본에 돌리니 **3 FAIL**(멀쩡한 본체에는 13 OK / 0 FAIL). ★교훈: **«차단형»은 «exit 1 을 쓴다»가 아니라 «그것을 부르는 잡이 있다»다.** 새 검사기를 만들면 그 자리에서 `grep -c <파일명> .gitlab-ci.yml` 을 한 번 쳐라. [닫힘:이 커밋]
