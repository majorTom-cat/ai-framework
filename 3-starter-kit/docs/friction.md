# 마찰 로그 — ★최신이 맨 위

> **적는 법**: `YYYY-MM-DD /스킬 — 무엇이 (한 줄)` 을 **아래 «지금 살아 있는 것» 맨 위**에 넣는다(끝에 붙이지 마라). 막힘·우회는 줄 머리에 **`[막힘]`**. 스킬 마무리 때 그 커밋에 동봉 — 별도 커밋·카드 불요.
> **닫는 법**: 고친 커밋이 그 줄 끝에 **`[닫힘:…]`** 을 붙인다. ⚠️기계가 보는 건 **`[닫힘:` 접두어뿐** — `[닫힘(①만):` 처럼 괄호를 끼우면 «안 닫힌 줄»로 계속 세어진다. 부가정보는 **콜론 뒤**에. 값 = `#카드번호` · 다른 커밋 해시 · `이 커밋` · `구기록` · `기록만`.
> ★**수리 후보가 여럿이면 «어디까지 넣었는지»를 적고, 안 넣은 것은 새 줄로 분가**시켜라 — 통째로 닫으면 남은 반쪽을 아무도 다시 안 본다.
> ★**적었으면 «마찰 수거» 카드 `#<수거카드번호>` 에 `@<공통개발자>` 멘션 한 줄**(카드가 없는 repo 면 «없음»). 이 파일은 아무 기계도 내용을 안 읽어서, 파일에만 적으면 **아무에게도 안 간다**. **집을 때도 한 줄** — 「이건 제가 집습니다」.
> ★**끝난 줄은 최근 5개만 남기고 보관으로**: `python3 scripts/archive-friction.py docs/friction.md docs/friction-보관.md 5` — **지우지 마라, 옮겨라**(마찰 줄은 규칙의 «영수증»이다).
> **수리 우선순위**: `[막힘]` = 즉시. 같은 것이 **두 번** 나오면 수리 후보. 나머지는 기록만.
> 경위·실측·«왜 이 규약인가» = `friction-규약.md` · 파일 종류별 읽기 처방 = `.claude/rules/verify.md` §4-b

---

## 지금 살아 있는 것 — 아직 안 고쳐진 줄
(없음)

## 최근에 끝난 것 (최신 5줄 — 그 앞은 `friction-보관.md`)
2026-09-17 /todo — ★**0-d «끝난 To-Do 비우기»가 닫힌 항목까지 사용자에게 묻고 있었다 — «아니오»가 나올 수 없는 질문이다.** bnsone 에서 닫힌 #249 의 To-Do 한 건이 세 세션 연속 질문에 올라 답 없이 쌓였고, 오너가 「왜 매번 손으로 지워야 하나」 → 확인 없이 지우고 한 줄 보고로 결정(bnsone-4c 발신 · 킷 세션에서 오너 「1」로 재확인). ⇒ `todo/SKILL.md` 0-d 본문·계약 줄 + `스캔상세.md` §3. 안전장치(`state != "opened"` 필터 · `--all` 금지 · 20건씩 · 재조회)는 그대로. 스킬이 한도에 차 있어 본문엔 짧은 줄만 두고 경위는 §3 으로(5,376자 이내 확인). ⚠️자동 모드 판정기가 이 편집을 «자기 수정»으로 두 번 막았다 — 오너가 모드를 바꾼 뒤 통과. [닫힘:이 커밋]
2026-09-16 /todo — ★★**머지 전 리뷰의 기본을 «매번 큰 워크플로 + 강도만 조절»에서 «독립 리뷰어 1명 + 직접 실행해 재현»으로 바꿨다**(오너 승인 「그래」 · bnsone !512 `6e9fe2f` 조회 확인). 근거는 bnsone 실측(25개 카드·리뷰 50회): 크게 돌린 리뷰의 확정은 대부분 사소했고(에이전트 40·25분 → 10건 전부 사소), «직접 실행해 재현하라»고 시킨 단일 리뷰가 중요한 결함을 잡았다(3회 → 재현 결함 6건). ⚠️기록이 성겨(토큰 20%·결함 위치 14%) **경향이지 증명이 아니다** — 그래서 기록 칸(방식·리뷰어 수·시간·토큰·확정·결함 위치 대상/비계, 모르면 «미제공»)을 필수로 박았다. ⇒ `/done` 4단계(+`allowed-tools` 에 `Agent` · 5,304자) · `단계상세.md` §9 재구성(기본 단일 리뷰 / `merge-review` 는 인증·권한·세션·비밀값·DB 마이그레이션·공용 계약·배포 설정만 / 기록 칸 / 9-10 수준 표는 «`merge-review` 를 쓸 때»로 한정) · `_reference/skill-tiers.md` 수준 표 머리에 적용 범위 · `CLAUDE.md` 작업 절차 한 줄(300자 이내) · 리더 안내서 AI 리뷰 정본 줄. `merge-review.js` 는 그대로(여전히 `level` 을 받는다). ★교훈: **9-10 의 «리뷰 수준은 카드가 정한다»는 «무엇으로 정하나»를 고쳤지만 «몇 명이 어떻게 보나»는 안 물었다** — 손잡이가 강도 하나라고 가정한 채였다. 강도를 낮춰도 비싼 건 그대로였고, 싸게 해도 잡는 건 «실행을 시켰나»였다. [닫힘:이 커밋]
2026-09-16 /todo — ★**배포처가 먼저 밟은 일반 교훈 6건을 킷에 끼워 넣었다**(bnsone `bnsone-ba` 발신 · !508 `23ab33c` · !511 `b4a92e7` — 두 해시 모두 `git log` 로 조회·main 포함 확인). ①`rules/verify.md` §3 — `$TMPDIR` 는 호출마다 달라질 수 있다 ②`/done` 단계상세 — 재리뷰는 «카드 대상 코드»가 바뀌었을 때만(가름선 «안 고치면 무엇이 깨지나») ③`/done` 단계상세 + `/todo` 0-c 포인터 + `스캔상세.md` §2-c 신설 — 워크트리를 지운 뒤 상대경로 조회가 조용히 0건(처방에 `--show-toplevel` 금지) ④`/inspect` — 되비침 판정은 `<script>` 를 걷어 낸 마크업에서 · 함정 §1 프리뷰 쿠키 `-b`/`-c` ⑤`_reference/worktree.md` — 저장 워크플로는 «세션을 켠 폴더»에서 읽힌다 · 링크하면 안 되는 이유 둘(앱 기동 불가 · 빌린 생성물이 이미 낡음) ⑥`/dev` 단계상세 §5-a 신설 — «재보고 정한다» 카드의 측정 전용 MR. ⚠️**현지화 차이 둘**: ⑤는 bnsone 이 «테스트만이면 링크 가능»으로 풀어 둔 것을 **킷의 «모르면 링크하지 마라»는 그대로 두고 근거로만** 넣었다 · ⑥의 `/dev` 본문 포인터는 **`dev/SKILL.md` 가 5,376자로 한도에 딱 차 있어** 넣지 않고, 본문이 이미 «§5 를 열어라»로 가리키는 자리의 맨 앞에 두었다. `§2-c` 는 `done/함정.md` 에도 있으나 파일이 달라 충돌 아님(참조 grep 확인). 추가만 27줄 · 삭제 0. [닫힘:이 커밋]
2026-09-16 /todo — ★★**«원문 불가침»을 올린 직후에도 «Removed 표시»가 같은 원문에 쓰라고 시키고 있었다 — 금지를 한 낱말(«표지판»)로만 찾았기 때문이다.** 앞 커밋은 «표지판»·«docs 수정 동봉»을 grep 해 자리를 셌는데, `rules/docs.md` 의 «요구사항이 삭제되면 절을 지우지 말고 Removed 표시»와 그것을 부르는 `CLAUDE.md` 기능 제거 순서 · `/change` 기능 삭제·금지 목록 · 리더 안내서 «재기준선 MR = docs 재작성 + Removed 표시»는 **다른 낱말로 같은 동작**(원문에 쓰기)을 시켰다. 커밋 직후 킷이 짚어 bnsone 세션에 물었고, 그쪽도 같은 결정으로 고쳤다(`fix/214-review-default`). ⇒ Removed 서식은 그대로 두고 **«원문 표시는 기획자 몫 — 우리는 그 STEP `_digest.md` 에 같은 서식으로 기록 + 통보»**로 네 자리(+리더 안내서 미러) 정정. 기획자 본인이 웹에서 고치는 안내(`ONBOARDING.md` 241행)는 **주체가 기획자라 그대로**. ★교훈: **금지 규칙을 넣을 땐 «금지할 낱말»이 아니라 «금지할 동작»으로 자리를 세라** — 같은 동작(원문에 쓰기)이 «표지판·수정 동봉·Removed 표시·재작성» 네 이름으로 흩어져 있었다. [닫힘:이 커밋]
2026-09-16 /todo — ★★★**킷이 40일 동안 오너 결정의 «정반대»를 기본값으로 배송했다 — «기획자 원문은 어떤 이유로도 고치지 않는다».** bnsone 오너가 2026-08-07 에 정한 것인데 **개인 메모에만** 있었고, 킷 `rules/docs.md`·`/docs` 5단계·`/change` 점검표는 «옛 원문 절 머리에 대체됨 표지판을 넣어라», `CLAUDE.md`·`/dev` 7단계·`/done` 6단계는 «스펙과 달라지면 docs 수정 동봉»을 시키고 있었다 — 기획자 안내서도 «AI가 표지판을 붙여 드려요»(167행)·«개발자가 문서도 같이 고친 것»(255행)이라 안내했다. bnsone 세션(`bnsone-ba`, !508·!511·#214)이 명문화하고 알려 왔고, 오너 확인: 「그게 팀 설정에 없고 내 개인 설정에만 있다고 몇 달이 지난 이제서야 고백해서 당장 고치라고 한거야」. ⇒ 킷 기본값을 뒤집었다: ①`rules/docs.md` 에 «원문 불가침» 머리 규칙 + 스펙 차이는 **`_digest.md` 기록 + 기획자 멘션**으로 · 상충도 **양쪽 digest + 통보**(표지판 금지) · 날짜 동률 규칙의 «지는 쪽에 표지판»도 걷음 ②`/docs` 세 동작 → 두 동작 · `/change` 점검표 · `/dev`·`/done` 문구(**같은 길이 이하로 바꿔 끼움** — 두 스킬 모두 한도에 차 있다) ③`CLAUDE.md` 두 줄(줄 수 199 그대로·300자 이내) ④기획자 안내서 167·255행(킷 + `2-team-dev` 미러, `cmp` 동일). **우리가 쓴 문서(ADR·digest·이력)의 표지판은 그대로** — 남은 «표지판» 언급을 파일별로 세어 전부 금지문·우리 문서 건임을 확인. ⚠️**안 가져온 것**: bnsone 의 CI 게이트 `planner-docs` 는 «우리 문서» 목록이 그 repo 경로에 박혀 있어 **배포처 설정으로 빼기 전엔 못 올린다** · «문서만 변경이면 게이트를 더하지 마라»는 bnsone 고유 결정. ★교훈: **오너 결정이 개인 메모에만 있으면 그걸 아는 세션은 규칙을 «조용히 건너뛰고», 모르는 세션은 규칙대로 «어긴다»** — 둘 다 사용자 눈에 안 보인다. **규칙과 사용자 지시가 갈리면 따르지도 건너뛰지도 말고 그 자리에서 드러내라.** [닫힘:이 커밋]
