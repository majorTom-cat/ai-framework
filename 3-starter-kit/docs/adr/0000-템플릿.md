# ADR-0000: (결정을 한 줄로 — 예: "DB는 PostgreSQL을 쓴다")

<!-- 채번: docs/adr/ 의 최대 번호+1 (4자리). 파일명: NNNN-제목-슬러그.md. 본문·카드에서 "ADR-NNNN"으로 참조.
     동시 작성으로 번호가 겹치면 늦게 머지되는 쪽이 재채번 (MR 충돌이 알려준다) -->

- **상태**: 채택 ← 나중에 뒤집으면 이 줄을 "대체됨(→ ADR-00NN)"으로 바꾸고, 새 번호로 새 파일을 쓴다 (이 파일 본문은 수정 금지)
- **날짜**: YYYY-MM-DD
- **결정자**: (이름)

## 배경 — 왜 결정이 필요했나
(2~3줄. 어떤 문제/갈림길이 있었나)

## 결정
(한 문단. "우리는 ~하기로 한다.")

## 검토한 대안
- 대안 A — 안 고른 이유
- 대안 B — 안 고른 이유

## 감수하는 것
(이 결정으로 잃는 것, 나중에 주의할 것)

<!-- 쓰는 기준: 되돌리기 어려운 결정만 (스택·공통 구조·모듈 경계 등). 쉽게 바꿀 수 있는 건 쓰지 않는다. 분량은 반 장, 20분. -->
