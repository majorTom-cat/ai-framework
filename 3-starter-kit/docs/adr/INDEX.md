# ADR 색인 — 지금 유효한 결정만

> ADR은 append-only라 파일이 쌓이고, 대체된 옛 결정과 현행 결정이 같은 키워드로 검색에 함께 걸린다.
> 그래서 이 색인이 **"지금 유효한 결정"의 단일 목록**이다 — ADR을 찾을 땐 `docs/adr/` 전체를 grep하지 말고 **이 표를 먼저** 보라(옛 결정을 현행으로 착각해 "복원"하는 사고를 막는다).
> 새 ADR을 쓰거나 기존 것을 대체하면 **이 표도 같은 커밋에서 갱신**한다.

| ADR | 결정 (한 줄) | 상태 | 날짜 |
| --- | --- | --- | --- |
| 0000 | (템플릿 — 실제 결정 아님) | — | — |
| 0001 | 비개발자(기획/디자이너) = Maintainer로 `docs/` 직접 편집·커밋 (길 A) | 현행 | 2026-07-13 |

<!-- 상태 = 현행 / 대체됨(→ ADR-NNNN). "대체됨" 행은 지우지 말고 남겨 이력을 보존하되, 현행이 무엇인지 화살표로 가리킨다. -->
