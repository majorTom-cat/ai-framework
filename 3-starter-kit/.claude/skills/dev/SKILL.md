---
name: dev
description: 기능 개발 전체 파이프라인 — 스펙 확인부터 구현·검증까지. 반나절 이상 걸리는 기능에 사용.
disable-model-invocation: true
argument-hint: "[이슈번호]"
allowed-tools: Bash(glab *) Bash(git *) Bash(npm *) Bash(docker compose *) Read Edit Write Glob Grep
---

이슈 내용 (자동 조회됨):
!`glab issue view "$0" 2>&1 | head -60`

최근 댓글 (기획 보충·반려 사유는 여기 있다):
!`glab issue view "$0" --comments 2>&1 | tail -40`

> **★위 조회가 «오류·빈 결과·엉뚱한 카드» 중 무엇이든이면 여기서 멈춘다** — 번호를 문맥으로 추측하지 마라(엉뚱한 카드에 브랜치·커밋이 생긴다). `/dev {번호}` 로 다시 부르라고 안내하고 종료.
> **인자 없이 불렀으면 `$0` 가 숫자가 아닌 값으로 확장돼 조회가 실패한다** — 그것도 이 정지 사유다(제목이 이 작업과 무관해 보이는 것도 마찬가지).
> ⚠️**이 판정은 AI 가 한다 — 셸 조건문으로 되돌리지 마라.** frontmatter `!` 명령은 **`allowed-tools` 에 선언한 명령 하나 + 파이프**여야 한다(정본 = CLAUDE.md '스킬 frontmatter `!` 명령').

# /dev — 이슈 #$0 기능 개발

> **계약** · 입력: 이슈번호(+지시) · 산출물: 구현 코드+테스트(feature 브랜치 커밋); 화면이면 /ui·/inspect 자동 · 검증: 테스트 green + /inspect → /done

> **예시**: `/dev 12` · `/dev 12 로그인은 소셜 로그인만` — **번호 뒤에 덧붙인 지시도 스펙에 반영**한다(전체 입력: `$ARGUMENTS`).

**이 스킬을 쓰면 안 되는 경우**: 반나절 미만의 작은 수정·버그·정리 → `/fix $0` (판별 기준은 /fix 참조)

> ★★**곁에 두 파일이 있다 — 해당 단계에 오면 반드시 읽어라**: `함정.md`(이렇게 하면 틀린다 + 경위 §1~§6) · `단계상세.md`(6·7단계 판정 순서 §1~§2). **이 안내를 여기 둔 이유**: 컴팩션은 스킬마다 **앞 5,000토큰만 남기고 뒤를 자르므로**(공식), 본문 뒤쪽에 있는 포인터는 대화가 길어지면 **포인터째로 사라진다.** 이 줄은 앞쪽이라 살아남는다.
> ★★**잘려도 이 셋은 지켜라**(본문 «❌ 금지» 절도 잘리는 자리다) — ①**남의 모듈을 수정하지 마라** ②**main 에서 직접 작업하지 마라** ③**게이트를 건너뛰지 마라(예외 없음)**. 그리고 «단순해서 스펙 확인 불필요»·«아마 되겠지, 테스트는 나중에»는 핑계다 — 완료 기준은 두 줄이면 되고, Seems right 는 증거가 아니다.

아래 체크리스트를 응답에 복사해 진행하며 체크하라. **⏸ 게이트에서만 멈추고, 그 사이는 연속 실행.**

```
- [ ] 1. 스펙 확인
- [ ] 2. ⏸ 스펙 승인   ← 작은 카드는 4와 묶어 한 번만
- [ ] 3. 계획
- [ ] 4. ⏸ 계획 go
- [ ] 5. 구현
- [ ] 6. 테스트 green
- [ ] 7. 화면 확인 안내 → /done
```

## 1. 스펙 확인
- **내 모듈인지 먼저 확인** — 카드의 `모듈:` 라벨을 CLAUDE.md 소유 표와 대조. 내 모듈이 아니면 착수 거부하고, 필요한 기능이면 **소유자 앞 인터페이스 카드(`/card`)**를 제안한다(남의 모듈은 그 모듈의 `index`로만 쓴다)
- **카드에 `대기(불명확)` 라벨이 있으면 착수 거부** — 수용 기준이 모호하다는 표시다. 작성자에게 무엇을 명확히 해야 하는지 되묻고, 명확해질 때까지 시작하지 마라
- **전 모듈이 얹히는 기반(인증·권한·알림·배치 같은 공통 인프라)이면 /dev를 시작하지 말고 `/design` 증분(⑤·⑥ 확장)을 먼저 제안한다** — 판정 기준: "이 카드가 실패하면 다른 모듈도 다시 만들어야 하나" (경위 `함정.md` §1)
  - ★**단 산출물이 «ADR 한 장»뿐인 결정 카드는 /design 이 아니라 `/dev` + `/adr` 로 간다** — 기반을 건드려도 코드가 안 나오면 /design 증분은 과하다(2026-08-26 실측: 갈 곳이 규정에 없어 매번 선례를 뒤졌다).
- ★**«수거함·감사·전수 점검» 유형이면 1단계를 «스펙 이해»가 아니라 «항목별 현재 상태 재실측»으로 바꿔라** — 이런 카드의 항목은 등록 뒤에 저절로 해소돼 있곤 하다(2026-08-24 실측: 18건 중 5건이 이미 해소). 산출물도 코드가 아니라 «고침 + 카드 + 판단»이다. **재실측 없이 착수하면 이미 없는 문제를 고친다.**
- **★본보기 모듈이 실제로 있는지 지금 확인한다** — CLAUDE.md의 `<본보기모듈>`이 **치환됐는지**, 그 폴더에 **실기능이 있는지**, 그리고 **이 카드가 쓸 축이 거기 실제로 있는지**까지 — 화면 카드면 화면·화면 테스트 수단, 연동 카드면 `index` 공개 계약, 메뉴·네비 카드면 렌더 방식(**해당 축만** 본다).
  - 미치환·빈 뼈대·쓸 축이 비었으면 **그 자리에서 공백으로 보고**하라(공통 개발자가 채울 몫). **속으로 메우지 마라** — 경위 `함정.md` §2.
  - 5단계에서 읽을 것이라도 **없다는 사실은 1단계에서 말한다.**
- **먼저 `git pull origin main`** — 최신 docs·코드로 시작한다 (변경(#N 대체) 카드면 특히: 로컬 docs가 옛 스펙일 수 있다)
- 이슈 본문·최근 댓글과 `docs/`의 관련 요구사항·시안을 읽고 — **카드가 가리키는 정본 경로의 해당 절만**(docs 전체를 통째로 읽지 마라: 무관한 문서는 노이즈가 되어 정확도를 떨어뜨린다) — **"이렇게 이해했다"를 5줄 이내로** 정리:
  무엇을 / 데이터는 / 화면은 / 실패 시엔 / 범위 밖은.
- 애매한 점은 추측 대신 **질문 목록**(질문마다 `➡️ 추천: …` 병기 — '사실'은 직접 조회하고 사람에겐 '결정'만) + 질문이 없어도 **가정 표면화**(`가정: 1)… 2)… — 틀리면 지금 고쳐주세요. 없으면 진행합니다.`).
  **재작업이 큰 가정(스키마·공개 인터페이스·모듈 간 계약)은 질문으로 승격**해 멈춘다

## 2. ⏸ 게이트: 사용자가 스펙 이해를 승인할 때까지 진행 금지
- 단축 조건: 카드에 docs 정본 참조 + 수용 기준이 완비돼 있고 **질문 목록이 비어 있으면**, "이해 요약 5줄 + **가정 목록**"을 카드 댓글로 남기고 통과해도 된다 (가정도 기록 없이는 통과 못 한다). **질문이 하나라도 있거나, 화면 상호작용·데이터 형태를 결정하는 가정이 있으면 반드시 멈춘다.**
- **★묶음 게이트 — 작은 카드는 게이트 1회**: 아래가 **전부** 맞으면 1·3단계를 이어서 하고 «이해 5줄 + 가정 + 계획»을 **한 화면에 함께** 보여준 뒤 **여기서 한 번만 선다**(4단계 게이트를 이 게이트가 겸한다. 승인은 "스펙+계획 go" 하나로 받는다).
  ① 질문 목록이 비어 있다 ② 계획이 작업 3개 이하 ③ 스키마·공개 `index`·모듈 간 계약을 바꾸지 않는다 ④ 화면 상호작용·데이터 형태를 결정하는 가정이 없다
  - ★**조건 ③ 예외 — 카드 본문에 요청 시그니처가 적힌 «인터페이스 카드»**: 인터페이스 카드는 정의상 항상 공개 `index` 를 바꾸므로 조건 ③ 때문에 **구조적으로 절대 못 묶인다.** 그런데 ③의 취지는 «계약 형태를 내가 새로 정하는 경우»이고, 인터페이스 카드는 **소비자가 이미 정해 온 것**이라 정반대다. 시그니처가 본문에 있고 질문이 0건이면 묶어도 된다(2026-09-03 bnsone 실측: 그 상태로 게이트를 두 번 섰다).
  - ★**`고위험` 라벨은 배제 사유가 아니다**(2026-08-28 오너 결정 "1"): 고위험은 «머지 리스크»(인증·결제·마이그레이션·설정)이고 이 게이트가 보는 건 «스펙 이해 리스크»다 — **둘은 직교한다.** 스펙이 세 줄인 고위험 카드에서 스펙·계획을 따로 승인받아 두 번 다 즉답이 나온 실측이 근거다.
  - 안전은 **머지 쪽 게이트**가 진다(AI 경고 리뷰·증적 3종·본인 실행 — CLAUDE.md '머지 등급'). 그건 그대로 선다.
  - **하나라도 아니거나 애매하면 지금까지대로 2회** — 게이트는 «줄이는 쪽»이 아니라 «맞추는 쪽»이 기본값이다.
  - 묶음 게이트에서 **스펙이 정정되면 계획을 다시 짜서 다시 보여준다** — 정정된 스펙 위에 옛 계획으로 착수 금지.

## 3. 계획
- 작업을 30분~2시간 크기로 쪼갠 목록 작성. 각 작업에 **정확한 파일 경로**와 완료 기준.
- ★**이전(migration)·개명 작업은 «호출부 N곳 — 그중 의미를 따져야 하는 것 M곳»을 계획 단위로 적어라.** 파일 경로 + 완료 기준만 요구하면 정작 시간을 먹는 일이 계획에 안 잡힌다(2026-08-26 실측: 호출부 35곳·26곳, 일부는 기계적 치환 불가라 하나씩 의미를 따져야 했다). M 이 크면 그것만으로 작업 한 칸이다.
- ★**그 목록은 내부 정확도용이다 — 사용자에게 보여줄 때는 따로 쓴다**: 파일명 나열이 아니라 «무엇이 어떻게 달라지는지»로(CLAUDE.md 세션 규칙).

## 4. ⏸ 게이트: 사용자가 "go" 할 때까지 코드 작성 금지 (브랜치도 만들지 않는다 — no면 빈 브랜치만 남는다)
- 2단계에서 **묶음 게이트로 승인받았으면 이 게이트는 이미 통과**다(같은 승인을 두 번 받지 마라).

## 5. 구현
- **착수 처리 먼저**: `git pull origin main` 재실행(게이트 대기 사이에 main이 앞섰을 수 있다)
  - **pull에 이 카드가 가리키는 정본(docs) 변경이 섞여 왔으면 정지**: 해당 절을 다시 읽고 스펙 이해(1·2단계)가 여전히 유효한지 확인, 달라졌으면 게이트로 복귀(기획 변경이면 `/change` 확인)
  - **`git status --short` 가 비어 있지 않으면 정지·보고**(더러운 트리 위 브랜치 금지)
  - ★**pull 로 남의 스키마 변경을 받았으면 ORM 생성물부터 재생성하라** — 안 하면 **시드가 먼저 깨진다**(2026-09-01 bnsone 실측: 지워진 컬럼을 참조해 P2022). 6단계에도 같은 안내가 있지만 **시드는 그보다 먼저 돈다.**
  - **카드가 이미 `진행중`+착수 댓글이 있는데 그 작업 브랜치가 내 로컬에 없으면 정지**(다른 세션·PC가 작업 중 의심 — 카드 댓글로 확인 후 진행) — **단 직전 `/module`로 생성한 모듈 스캐폴드는 예외**(곧 이 브랜치에 함께 커밋할 정당한 미커밋 파일이다)
  - 브랜치 `git checkout -b feature/$0-{슬러그}` + **라벨 전환을 명령으로**: `glab issue update $0 --label 진행중 </dev/null` → `glab issue view $0`로 반영 재확인
    (**명령·재확인까지가 전환이다** — 산문 지시는 안 지켜졌다, 경위 `함정.md` §3) + 착수 댓글 한 줄(base main 해시 포함 권장 — 상태 전이표의 "착수 시"가 바로 지금이다). 모듈 스캐폴드가 있으면 이 브랜치 첫 커밋에 함께 담는다
- **고위험 카드면**(`고위험` 라벨, 또는 CLAUDE.md '머지 등급'의 고위험 유형 해당) **지금 "경고 레인"임을 알려라**:
  - "머지 때 **AI 경고 리뷰 + 증적 3종**을 남기고 본인이 게이트를 실행합니다(동료 승인은 선택)"를 한 줄로 알린다. ★**승인자를 정하려고 묻고 멈추지 마라.**
  - 카드에 `"고위험 카드 착수 — 머지 때 경고 레인(AI 경고 리뷰 + 증적 3종)"` 예고 댓글을 남긴다(실제 절차는 /done 7단계). 동료를 붙이기로 했으면 함께 @멘션(선택).
- **의존 카드라면** 두 갈래:
  ① 선행이 **이미 머지됨** → main에 선행 커밋이 있는지 확인하고 main에서 착수. 선행이 검수 반려 중이면 그 여파(스펙이 또 바뀔 수 있음)를 카드에 기록하고 진행 여부를 판단
  ② 선행이 **미머지** → 선행 feature 브랜치에서 분기 + 카드에 "base: feature/{선행} — 머지는 선행 먼저" 기록
- **먼저 본보기 모듈(`src/modules/<본보기모듈>`)의 해당 패턴(파일 위치·`index` 노출·테스트 형태·플래그 사용법)을 읽고 그 관례를 따라라.**
  이탈은 CLAUDE.md '작업 절차' 관례 규칙대로(의식적 이탈+이유 기록). 억지로 맞추다 이상해지는 게 베끼기의 실패 모드다.
- **구현 수단은 사다리 순서로**: 기존 코드(shared·본보기) 재사용 → 표준 라이브러리 → 플랫폼 내장(`<input type=date>`·CSS·DB 제약) → 기설치 의존성. **몇 줄로 되는 일에 새 의존성 금지**(추가 = package.json 공통 영역 공지 대상). 없으면 자기 모듈 안에 만든다(shared 승격은 2개 모듈 필요 시). 단 **검증·에러 처리·보안·접근성은 줄이는 대상이 아니다.**
- 과잉 추상화 금지: 구현 1개뿐인 인터페이스·제품 1개뿐인 팩토리·바뀔 일 없는 값의 설정화 — 둘째가 생길 때 만든다(shared 승격 기준과 동형). 의도적으로 한계를 남기면 주석 한 줄로 한계+승급 경로 명기(`// 한계: 전역 락 — 처리량 문제 시 계정별 락`), 크면 카드로.
- 상대 모듈 index 확인은 모듈 지도(`src/modules/_MODULE_MAP.md`)가 있으면 grep 대신 지도부터 — 재생성 `npm run boundaries -- --map`(코드와 다르면 재생성이 정답. 지도가 없거나 스캐폴드 전이면 `함정.md` §4).
- 스키마 변경이 있으면: 마이그레이션(날짜시각 파일명 — ★**규칙 형식이 도구 기본과 다르면 생성 직후 rename**, 어느 쪽이든 `ls | sort`로 **내 것이 맨 뒤인지** 확인. rules/migrations.md) + 멱등 시드 갱신 동봉(표본은 게이트 변수 뒤 — 같은 문서).
  **★마이그레이션 카드는 `고위험` 라벨 + 착수 전 «충돌 없음» 확인**(전역 직렬 자산 — 두 사람이 동시에 만들면 나중 것 순서가 꼬여 스키마가 깨진다).
  **확인 절차·공지 조건은 `rules/migrations.md` '착수 공지 = 조건부'가 정본** — 요지: main 최신 마이그레이션과 열린 MR 둘 다 깨끗하면 **공지 없이 착수하고 확인 결과를 카드에 한 줄**, 하나라도 걸리거나 조회가 실패하면 **공지**(2026-08-31 오너 결정 "2" — 무조건 공지는 3연속 거절돼 무력화되고 있었다).
- **되돌리기 어려운 결정을 이 작업에서 새로 정하게 되면**(모듈 경계·두 모듈 간 계약·공개 API 형태·스키마 방향) → **`/adr` 로 기록**하고 진행한다(안 남기면 다음 세션 AI가 옛 방향으로 "복원"한다). 두 모듈 계약이면 양쪽 소유자 확인 후.

## 6. 테스트
- 층별 테스트 작성(본보기의 테스트 형태 참조) → `npm test` → **green이 될 때까지 반복. red인 채 다음 단계 금지.**
  (명령이 repo CLAUDE.md '실행·테스트' 절과 다르면 **CLAUDE.md가 정본**. 변경에 `.gitlab-ci.yml` 있으면 `glab ci lint` 도 — 무효 CI는 테스트가 못 잡는다)
- **테스트는 공개 경계에 건다**(모듈 `index` 공개 함수·API 응답·화면 동작) · **동어반복 테스트 금지**(기대값은 **독립 출처**에서) · **공유 DB 에서는 내가 만든 행으로 단언을 좁혀라**.
- ★**red 를 고치기 전에 «의존이 떠 있나 · 내 탓인가»를 먼저 답하라** — 컨테이너·ORM 생성물·미설치 패키지가 원인인 red 를 **코드 결함처럼 읽는 사고가 반복됐다**(실측 4건). 판정 순서와 경위는 **`단계상세.md` §1 을 읽어라.**
- ★★**초록은 케이스가 유효하다는 증거가 아니다 — «이 케이스가 결함을 잡는가»를 따로 확인하라.** 케이스 **수**는 근거가 못 된다(2026-08-28 두 번 실측). **방법은 `함정.md` §6 을 지금 읽고 그대로 하라** — ①겨눈다 ②지운다 ③판정한다 ④되돌린다, 넷이 각각 다른 방식으로 틀렸다(실측 6건. 여기서 짐작하지 마라).

## 7. 마무리
- ★**로컬 화면을 보기 전에 재빌드 + 브라우저 하드 리로드** — 안 하면 코드 변경이 화면에 안 나타나 «구현이 틀렸다»로 오독하고, 옛 페이지에서 누른 저장이 **조용히 실패**한다. 경위는 **`단계상세.md` §2.**
- 화면이 있는 카드면 **`/ui $0` → `/inspect $0`**(테스트 green 은 코드 층만 본다). ★**디자인 정본이 «채택 대기»면 `/ui` 는 채택 후로 미루고 `/inspect` 만** 돌리고, **미룬 사실을 카드 댓글 한 줄로 남겨라**(`단계상세.md` §2).
- **모든 변경을 커밋**(첫 줄 `#$0 무엇을 했는지`) — 미커밋 작업트리인 채 `/done` 으로 가지 마라.
  - ★**변경 파일을 경로로 지목해 add 하라 — `git add -A`·`add .` 금지**: 작업트리엔 남의 미추적 파일·잔해가 있을 수 있다(실사고 경위 `함정.md` §5)
- 구현이 docs와 달라진 점이 있으면 **docs 수정을 이 브랜치에 동봉**. 완료 보고에는 **증거**(테스트 출력 요약 + 무엇을 실행해 어떻게 확인했는지)를 붙여라 — "될 겁니다"는 증거가 아니다.
- ★**스킬·규칙·킷 문서에 막혔거나 우회했거나 헷갈렸으면 스스로 남긴다**(사용자에게 묻지 말고 판단): `docs/friction.md` 한 줄(`날짜 /스킬 — 무엇이`, 이 커밋 동봉) — **막힘·우회는 줄 머리 `[막힘]` 태그**. **`[막힘]`이면 «마찰 수거» 카드에 멘션 댓글 한 줄까지가 한 벌이다**(번호는 friction.md 머리) — 파일에만 적으면 소유자에게 **안 간다**(2026-09-04 실측).
- 사용자에게: "완료했습니다. `/done $0` 으로 머지하고, 배포 후 화면을 직접 확인하세요."

## ❌ 금지 — 그 순간 드는 핑계와 반박까지

- 남의 모듈 수정 · main에서 직접 작업 · 게이트 건너뛰기 (예외 없음)

| 하고 싶어지는 말 | 반박 |
| --- | --- |
| "단순해서 스펙 확인 불필요" | 단순한 작업도 완료 기준은 필요하다 — 두 줄이면 된다 |
| "본보기 안 봐도 안다" | 네가 아는 방식 ≠ 팀 패턴 — 변종 하나가 발산의 시작 |
| "스펙에 없지만 좋을 것 같아 추가하자" | 아이디어는 카드로 — 임의 추가는 검수 기준이 없어 검수 불가 |
| "아마 되겠지, 테스트는 나중에" | Seems right는 증거가 아니다 — 출력·실행 결과만 증거 |
