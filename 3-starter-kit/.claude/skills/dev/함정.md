# /dev 함정 목록 — 실측 경위 모음 (본문이 가리킬 때 읽는다)

> 여기 있는 것은 전부 **실제로 겪은 실패와 그 경위**다. 본문이 "경위 `함정.md` §N"이라고 가리키면 해당 절만 읽어라.

## §1 기반 카드를 /design 없이 착수한 결과 (1단계)

- 전 모듈이 얹히는 공통 인프라(인증·권한·알림·배치)를 **설계 한 줄 위에서** 구현하면, 즉흥 결정이 리뷰가 끝난 뒤에야 잡힌다.
- bnsone #63 실측: **Blocker 5건**, 병렬로 진행하던 구현을 되돌렸다.
- 그래서 판정 기준은 "이 카드가 실패하면 다른 모듈도 다시 만들어야 하나" — 그렇다면 `/design` 증분(⑤·⑥ 확장)이 먼저다.

## §2 본보기 모듈 — "폴더가 있나"에서 멈추면 늦는다 (1단계)

- **미치환·빈 뼈대를 속으로 메운 사례**: 2026-07-28 파일럿 — `<본보기모듈>`이 치환되지 않았는데 "아마 저 모듈이겠지"로 진행했다. 공백은 공통 개발자가 채울 몫이라 **그 자리에서 보고**했어야 한다.
- **폴더는 있는데 쓸 축이 빈 사례**: bnsone #51 — 착수 직전에야 **빈 축 3개**(화면 테스트 수단·`index` 공개 계약·메뉴 렌더 방식)를 발견했다.
- 그래서 확인은 폴더 유무가 아니라 **이 카드가 실제로 베낄 축이 거기 있는가**까지 간다. 없다는 사실은 5단계에서 읽을 것이라도 **1단계에서 말한다.**

## §3 라벨 전환은 산문 지시로는 안 된다 (5단계)

- "전환하라"는 산문 지시는 지켜지지 않았다 — **라벨 커버리지 9.5%** 실측(2026-08-06).
- 그래서 `glab issue update $0 --label 진행중 </dev/null` **명령**으로 바꾸고, `glab issue view $0`로 **재확인**하는 데까지가 전환이다. 명령 성공 출력은 반영의 증거가 아니다.

## §4 모듈 지도가 아직 없을 때 (5단계)

- `npm run boundaries -- --map`은 **스캐폴드가 끝난 repo** 기준이다(package.json에 `boundaries`가 있어야 한다).
- 골격 생성 전이라 그 스크립트가 없으면 킷 동봉본을 직접 부른다 — `scripts/check-boundaries*.cjs` 중 **이 스택 변형**에 `--map`.
- 지도와 코드가 다르면 **재생성이 정답**이다(지도를 손으로 고치지 마라).

## §5 `git add -A`가 남의 파일을 main에 쓸어 담은 사고 (7단계)

- bnsone 실사고 `15c882c` — 충돌 해결 중 `git add -A`가 무관한 `.idea`·기획 파일까지 main에 담았다.
- 작업트리엔 **남의 미추적 파일·잔해**가 있을 수 있다. 파일명 NFC 중복 사고의 유입 경로이기도 하다.
- 그래서 **변경 파일을 경로로 지목해 add** 한다. `git add -A`·`add .`는 금지다(/done 7단계도 같은 규칙).
