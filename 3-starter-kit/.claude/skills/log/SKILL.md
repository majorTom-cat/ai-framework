---
name: log
description: 변경 이력 조회 — 모듈·공통·파일을 누가 언제 무엇을 왜 바꿨는지 요약. "이거 언제 바뀌었지?" 할 때 사용.
argument-hint: "[모듈명|shared|파일경로] [기간(선택, 예: 2주)]"
allowed-tools: Bash(git log *) Bash(git blame *) Bash(git shortlog *) Bash(glab *)
---

# /log — 변경 이력 조회

> **계약** · 입력: 모듈/파일명(+기간) · 산출물: 이력 표(날짜·누가·무엇[카드#]·왜) — 부작용 없음 · 검증: 조회형 — 별도 없음

> **예시**: `/log board` · `/log board 지난주` (모듈·파일 + 기간 선택)

대상: $0 / 기간: $1 (없으면 최근 1개월)

1. 대상을 경로로 해석: 모듈명 → `src/modules/{이름}` · `shared` → `src/shared` · 그 외는 경로 그대로
2. `git log --since="{기간}" --stat -- {경로}` 로 이력 수집 (기여자 집계가 필요하면 `git shortlog -sn`)
3. **사람이 읽는 표로 요약**:
   | 날짜 | 누가 | 무엇을 (카드 #번호) | 왜 |
   - "왜"가 커밋 메시지에 없으면 카드 번호로 `glab issue view` 를 조회해 보강
4. 특정 줄·함수가 언제 바뀌었는지 물으면 `git blame` 사용
5. 마지막에 한 줄 요약: "이 기간 {대상}의 주요 변화는 ~"

## ❌ 금지
- 기록에 없는 이유를 추측으로 채우기 — 없으면 "기록 없음"으로 표시 (커밋에 카드 번호가 빠진 경우 그 사실도 알려줘라)
