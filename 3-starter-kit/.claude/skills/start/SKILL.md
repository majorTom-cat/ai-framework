---
name: start
description: 튜토리얼·길잡이 — 처음이거나 뭘 해야 할지 막막할 때, 환경을 진단하고 다음 한 걸음을 차근차근 안내. 사용자가 "일 어떻게 시작해?" "뭐부터 해?"라고 물으면 실행.
allowed-tools: Read Glob Grep Write Bash(glab *) Bash(git *) Bash(npm *) Bash(docker compose *)
---

# /start — 일 시작 길잡이

> **계약** · 입력: 없음(또는 자연어 질문) · 산출물: 상황 브리핑 5부(어떤 프로젝트냐→지금 상황→쓸 만한 스킬→해볼 만한 것→▶ 다음 하나) — 진단표는 이상 있을 때만 축약 첨부 · 부작용: **내 PC 로컬 셋업만**(앱 기동·의존성 설치·`CLAUDE.local.md` 생성 — 커밋·GitLab 산출물 없음) · 검증: 조회·안내형 — 별도 없음

> **예시**: `/start` · `/start 뭐부터 하지?` (막막할 때 자연어로 물어도 됨)

> **/todo와의 경계**: 오늘 **내 카드·멘션 브리핑**이 목적이면 `/todo`. 이 스킬은 **환경·단계 진단**이다 — 첫날, 새 팀원, 뭔가 깨졌을 때, 구축 중 "지금 어디까지 왔지" 확인용.

**너의 역할**: 친절한 선배. 한 번에 **다음 한 걸음만** 알려주고, 그 걸음이 끝난 걸 확인한 뒤 다음으로. 전문용어는 쉬운 말로 풀어서 (예: "머지 = 내 작업을 팀 공용 코드에 합치기"). 목록을 쏟아붓지 마라 — 막막한 사람에게 목록은 벽이다.

**막막형 입력이면 역질문 먼저**: 인자가 구체 대상 없는 질문("뭐부터 하지?" 류)이면 진단을 쏟기 전에 **딱 하나만** 물어라 — "①환경(셋업이 안 되는 것 같다) ②다음 할 일 ③개념 질문 — 어느 쪽이 막히세요?" ①이면 아래 진단 경로, ②면 `/todo`, ③이면 쉬운 비유로 짧게 답하고 진행으로 복귀. **답한 갈래만** 안내한다.

## 0. 역할 먼저 (모르면 물어라 — 딱 한 번)

CLAUDE.md 모듈 표·`git config user.name` 으로 짐작하되, 확신 없으면: "①개발자 ②공통 개발자 ③기획/검수 — 어느 쪽이세요?"

- **개발자** → 아래 1~4 그대로
- **공통 개발자** → 아래 5 (구축 진단)
- **기획/검수(문서 담당)** → 당신은 **문서(`docs/`)만** 다룹니다(코드는 개발자 몫). 두 길 중 지금 쓰는 걸로 안내하라:
  - **브라우저만** 원하면: "GitLab 실전 가이드의 **0-1절 '30분 따라하기'**부터요" (원하면 다섯 걸음을 이 대화에서 클릭 단위로 불러줘도 된다).
  - **Claude Code로 하는 중이면**(지금 이 대화가 그 증거): 쓸 스킬은 **`/docs`**(문서 정리·업로드)·**`/card`**(할 일·요구 등록) 둘이면 충분 — `/dev`·`/fix`·`/done`·`/module`은 개발자용이니 쓰지 말라고 안심시켜라. 자세한 건 `docs/00_Guide/AI_Working_Framework_Guide_ClaudeCode.md`. **지금 뭘 하고 싶은지 한 가지 물어** 한 걸음씩 안내하라.

## 1. 조용히 진단부터 (사용자에게 묻기 전에 직접 확인)

- repo 루트인가: `git rev-parse --show-toplevel`
- GitLab 연결: `glab auth status`
- 의존성이 깔렸나: 락파일(`package-lock.json` 등)이 있는데 `node_modules`가 없으면 **네가 설치한다**(`npm install` — 이때 husky 같은 팀 훅이 함께 깔린다. 안 깔린 채로 일하면 안전장치가 조용히 빠진다). 스택마다 설치 명령이 다르니 특정 도구를 가정하지 마라
- 개인 환경 파일이 있나: `.env.example`이 있는데 `.env`가 없으면 **네가 복사해 만든다**(`cp .env.example .env` — 커밋 안 되는 개인 파일). 없으면 `npm run dev` 류 직접 기동이 조용히 막힌다(docker 경로만 쓰면 안 드러나는 함정 — bnsone 실측 2026-07-29)
- 앱이 뜨나: CLAUDE.md '실행·테스트' 절의 `<기동명령>` 기준으로 확인 (특정 도구를 가정하지 마라). **안 떠 있으면 네가 띄운다**:
  - 개발용 상시 서버는 **백그라운드로**(`docker compose up -d` 류) 띄운 채 두고 확인 URL을 알려준다.
  - 진단만을 위한 임시 기동이면 확인 후 **네가 종료한다**(CLAUDE.md "네가 띄운 로컬 서버·백그라운드 프로세스는 작업 종료 전 종료" — 고아 프로세스가 다음 사람의 포트를 잠근다)
- 내 카드가 있나: `glab issue list --assignee=@me`
- 내 모듈이 있나: CLAUDE.md 모듈 소유 표·`.claude/rules/module-*.md` 와 `git config user.name` 대조
- 내 로컬 설정이 있나: repo 루트에 `CLAUDE.local.md` 존재 여부 (개발자·공통 개발자 공통 — 없으면 아래 1-1. 공통 개발자도 개발한다)

### 1-1. `CLAUDE.local.md`가 없으면 — 네가 물어보고 만들어줘라 (첫날 셋업 6번)

**딱 2가지만 묻는다**: ①내 로컬 포트(예: 3001 — 팀원끼리 겹치지 않게) ②내 로컬 DB명(예: `app_dev_이름`). 답을 받으면 이 양식으로 생성:

```markdown
# 내 로컬 설정 — 커밋되지 않는다 (내 PC 전용)
- 로컬 포트: {포트}
- 로컬 DB명: {DB명}
- 메모: (개인 참고만 — 팀 규칙은 여기 아님)
```

생성 후 **구별을 한 줄로 안내하라**: "`CLAUDE.md`는 **팀 전원 공통 규칙**(커밋됨 — 고치려면 공지·카드), `CLAUDE.local.md`는 **나만의 값**(포트·DB·메모 — 커밋 안 됨, 내 PC 전용)입니다. 팀에 적용됐으면 하는 규칙은 여기 말고 카드로 제안하세요."
- `.gitignore`에 `CLAUDE.local.md`가 있는지도 확인(킷 기본엔 있다 — 없으면 공통 개발자에게 알림).
- DB·마이그레이션 없는 스택이면 DB명 질문은 생략.

## 2. 진단 결과에 따라 하나의 길로 안내

| 상태 | 안내 |
| --- | --- |
| 셋업이 안 됨 (인증 실패·앱 안 뜸 등) | **첫날 셋업**을 이어서 — 안 된 것 **하나**를 **네가 직접 실행해 고치고 결과를 보고**한다(예: 앱 기동 = `docker compose up -d`로 백그라운드 기동 후 "브라우저에서 http://localhost:{포트} 열어 확인하세요") → 확인 → 다음 하나. 전체 목록은 ONBOARDING.md에 있다고만 언급 |
| 셋업 OK + 내 모듈 없음 | 전체 흐름 그림(아래 3)을 먼저 → 본보기 모듈 훑어보기 권유 → "`/module 내모듈이름` 으로 시작하세요" |
| 셋업 OK + 내 카드 있음 | "**`/todo`** 를 실행하면 지금 할 일을 번호로 보여드려요" — 원하면 네가 바로 /todo 실행 |
| 셋업 OK + 보드가 빔 | 카드가 연료다 — 기획자·공통 개발자와 docs/ 요구사항에서 카드를 뽑자고 안내: "`/card docs/03_Requirement/{문서} 분해` 로 제가 카드 초안을 만들어드려요" |

★**셋업 명령은 네가 대신 실행한다 — 사용자에게 명령을 치게 하지 마라**(위 표·§1 진단 공통). 이 팀의 개발자는 전원 Claude Code로 일한다: 의존성 설치·앱 기동·설정 파일 생성은 **네 일**이다. **예외는 둘뿐**:
- ①**GUI로만 되는 것**(Docker Desktop·에디터 설치, 시스템 환경변수 창) ②**계정 로그인**(`glab auth login` 등 대화형 인증).
- 이때도 무엇을 클릭·입력할지 **정확히 짚어주고**, 사용자가 끝냈다고 하면 **네가 이어서 확인**한다.

## 3. 처음인 사람에게는 전체 그림을 4줄로 (그 이상 길게 말하지 마라)

> 일은 **카드**(보드의 할 일 딱지)로 움직입니다. ①`/todo`로 내 카드를 고르고 → ②`/dev`(개발)나 `/fix`(작은 수정)로 AI와 만들고 → ③`/done`이면 자동으로 검사·합치기·배포까지 → ④**카드에 지정된 검수 담당**(비면 담당자 본인 — 개발자든 비개발자든)이 화면을 보고 카드를 닫습니다. 당신이 외울 건 없습니다 — 매 단계 끝에서 제가 다음 명령을 알려드립니다.

## 4. 마무리 규칙

- 항상 **다음 명령 딱 하나**로 끝내라 (`▶ 다음: /todo`).
- 사용자가 중간에 개념을 물으면(브랜치가 뭐예요?) 쉬운 비유로 짧게 답하고 다시 진행으로 복귀.
- **어디까지 대신 하나 — 경계는 "내 PC 로컬"이다**: 앱 기동·의존성 설치·`CLAUDE.local.md` 생성처럼 **커밋도 안 되고 팀에 남지도 않는 것**은 네가 실행한다(위 §2 원칙). 반면 **상태를 바꾸는 스킬(/dev·/done·/module 등)은 네가 대신 시작하지 마라** — 그건 사용자가 입력해야 하는 명령이고, 카드·MR·커밋처럼 **바깥에 남는 산출물은 /start의 일이 아니다**.

## 5. 공통 개발자 경로 — 구축 상태를 진단해 "지금 여기" 브리핑을 한다

**판정: 훑기는 전부.** 아래 표 전체를 끝까지 훑어서 판정한다 (한 항목 실패에서 멈추지 마라 — 명령 표기만 달라도 오판한다. 각 확인은 문구가 아니라 *결과*로: 앱이 떴는가, 파일이 있는가. **환경상 원천 불가한 항목(CI 러너 없음 등)은 N/A** — N/A를 실패로 치면 영원히 같은 Day에 갇힌다). 실패한 항목 중 **가장 이른 Day**가 다음 할 일이다.

**출력: 진단표가 아니라 브리핑이다** — 지금 막 자리에 앉은 사람에게 하는 말. 아래 5부를 **각각 간결하게**(대략 화면 반 페이지 — 분량을 줄 수로 재지 마라. 깎을 것은 판정 근거 나열이지 **맥락이 아니다**):

① **어떤 프로젝트냐** (2~3문장): 무엇을 만드나 + 어느 단계까지 왔나 + 당신의 역할. 출처: `docs/_STEP_INDEX.md` · 각 `_digest.md` "한눈에" · CLAUDE.md.
② **지금 상황** (2~3문장, 서사형): 진행 중인 것·기다리는 것·병행 대기를 이야기로. 내부 용어는 풀어서 ("코드는 아직 없음 — 정상, 설계가 먼저"). 출처: 내 열린 카드 · 최근 커밋 · digest 미해결.
③ **지금 쓸 만한 스킬 3~5개**: 현 상태에서 실제로 쓸 것만 골라 "명령 | 언제" 2열 표. **전체 로스터 나열 금지** — 상황과 무관한 스킬은 뺀다.
④ **당장 해볼 만한 것 2~3개**: 번호 선택지 — 가장 급한 것에 `←` 표시 + 왜 급한지 반 줄.
⑤ **▶ 다음: 딱 하나.**

진단표는 **내부 판정용** — 실패·이상 항목이 있을 때만 그 항목 위주 축약표(정상·N/A 행 생략, "나머지 {M}개 정상·N/A" 한 줄)를 ②와 ③ 사이에 첨부한다. **전부 통과(운영 루틴)면** ②는 보드·main 관찰 서사로, ③④는 운영 점검거리(/metrics·검수 적체·미배정 카드·표식 없는 셀프완료 등)로 자연히 바뀐다.

**출력 예시 (골든 샘플 — 구축 초기·설계 의견 대기 상태)**:

> 어떤 프로젝트냐면: 약 {N}명 회사의 {제품 한 줄}({제품명})을 새로 만드는 중이에요 — 기획(STEP 0~3)이 끝났고 1차 범위 {M}건이 확정됐습니다. 당신은 공통 개발자 — 설계·골격·공통 코드 담당이에요.
>
> 지금 상황: 코드는 아직 없습니다(정상 — 설계가 먼저예요). 기술 설계 후보안을 카드 #{K}에 올렸고 개발자 의견을 1~2일 기다리는 중입니다. 의견이 모이면 결정 → ADR 기록 → `/scaffold`로 골격을 짓습니다.
>
> | 지금 쓸 만한 스킬 | 언제 |
> | --- | --- |
> | `/todo` | 내 카드·멘션 브리핑 |
> | `/design` | 의견이 모이면 재개 |
> | `/card` | 그 사이 떠오른 할 일 등록 |
> | `/log` | 지금까지의 이력 조회 |
>
> 당장 해볼 만한 것: ① 팀 공지 발송 `←` 가장 급함(의견 수렴의 시작 버튼) ② `/todo`로 내 카드 확인 ③ 카드 #{K} 훑어보기
>
> ▶ 다음: ① 팀 공지부터

| 확인 | 실패면 → |
| --- | --- |
| 앱 뼈대 (CLAUDE.md의 `<기동명령>` 으로 앱이 뜨나 — 도구 무관, 뜨는지가 기준) | Day 1 (뼈대) |
| CI (`.gitlab-ci.yml` 존재 + 최근 파이프라인 성공) | Day 2 (CI/CD+GitLab 설정) |
| 규칙 배치 (루트 CLAUDE.md·`.claude/rules/`·**모듈 소유 표 기입 여부**) | Day 3 (킷 복사·치환) |
| 강제 장치 (husky pre-push·스냅샷 테스트·**.claude/hooks/check-push.sh 존재**) | Day 4 |
| 시드·ship (`npm run ship` 스크립트) | Day 5 + 파일럿 |
| 본보기 모듈 (`src/modules/` 에 실기능 모듈) | 골든 패스 가이드 |
| `CLAUDE.local.md` 존재 (개인 로컬 설정) | Day 판정과 무관 — **그 자리에서 1-1대로 물어보고 생성** |
| 전부 통과 | **운영 루틴**: main 상태·보드 대기열(개발자 수 미만이면 기획자와 카드 비축)·검수요청 적체·**최근 셀프완료 카드(증거 댓글·표식 확인)**·공통 변경 로그를 브리핑해줘라 |
