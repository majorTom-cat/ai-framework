---
paths: src/modules/{모듈이름}/**
---
# 모듈 {모듈이름} — 소유자: {이름}

> ★**이 파일은 조건부다** — 그 모듈 파일을 **Read 도구로 열 때만** 걸린다(셸로 읽으면 무음. `_reference/rules-loading.md`).
> 조건부로 남긴 이유: 모듈 수만큼 늘어나 전부 상시 로드하면 컨텍스트가 감당이 안 된다.
> **그래서 «남의 모듈 수정 금지»라는 하드 규칙은 여기 두지 마라 — CLAUDE.md '소유 경계'(상시) + CI `boundaries` 잡(기계)이 정본이다.** 이 파일은 그 모듈의 보강 메모다.
<!-- 이 파일을 module-{이름}.md 로 복사해 소유자가 채운다. 사실만, 짧게. -->

- 무엇을 하는 모듈: <한 줄>
- 데이터: <테이블들> (schema.ts 참조)
- 화면 규칙: <목록 페이지네이션 개수, 특이 UX 등>
- 이 모듈만의 주의점: <있으면>
- 구현 패턴은 본보기 모듈(src/modules/<본보기모듈>)을 따른다
