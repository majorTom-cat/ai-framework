# AI-CONTEXT — 세션 넘어 이어가는 맥락 (메모리 이식본)

> **이건 무엇**: Claude Code의 로컬 메모리(`~/.claude/.../memory`)는 PC를 안 따라간다. 그래서 이 repo에서 작업할 때 새 세션(특히 다른 PC/맥)이 읽고 시작하도록, 그 메모리의 핵심을 여기 옮겨 둔다. **프레임워크 작업 시 이 파일 + `HANDOFF.md`부터 읽어라.**
> 갱신: 새 교훈이 생기면 여기 한 줄 추가(로컬 메모리와 이중 관리 — 이 repo가 정본).

## 1. 사용자·협업 방식 (yskim)
- **yskim = BNSOne 오너/공통 개발자.** 사내 AI 협업 프레임워크를 도입 중. (역할명: "리더" 아님 → **공통 개발자**. 2026-07-15 변경, 단 과거 evidence 인용·파일명은 보존.)
- **존댓말로 답한다.** 규칙 문서는 AI 지시문이라 하다체지만, 사용자에겐 존댓말.
- **개념·트레이드오프를 쉬운 말로 먼저 설명하고** 선택지를 준다. 장황함 싫어함 — 짧고 명확하게.
- **자동 push 금지.** 바꾼 건 보여주고 **명시적 "올려/push" 지시를 기다린다.** 특히 외부·비가역 행동(GitHub 공개, bnsone 반영)은 확인 먼저.
- **문서 비대화로 환각을 겪은 경험** → 린 컨텍스트 우선. 가이드·규칙은 군더더기 없이(부연은 OK, 잔재·중복·불필요는 컷). 중요 기능은 설명이 무게를 전달.

## 2. 핵심 교훈 (실패에서 나온 것 — 반복 금지)
- **"없다/안 됐다"를 근거 없이 단정 금지.** grep 0건·서브에이전트 요약·플레이스홀더 빈칸은 부재의 근거가 아니다(패턴오류·기록누락·추론일 수 있음). 부재를 주장하려면 **그 파일을 연 결과/그 API 호출 결과**를 대라. (bnsone 하루 4회 실측.)
- **조언·지시문 전에 상태를 실측하라**(git log·파일 read). 추정 안내로 산출물 헛일 2회.
- **규칙 축적은 반패턴 — 추가 1건당 삭제/훅전환 후보 1건.** 발동지점이 없어 규칙이 단조 증가했던 게 뿌리. 지금은 `scripts/check-density.sh` + CI `density-check`가 초과 줄을 매 MR마다 나열해 **가시화**한다(CLAUDE.md 300자/줄·스킬 500자/줄). **단 `allow_failure: true`라 경고일 뿐 머지를 막지 않는다 — "기계로 강제"가 아니다.** 실제 준수는 사람·AI가 `bash scripts/check-density.sh`를 직접 돌려 지킨다. 자주 위반되는 고비용 규칙은 문장 말고 **훅/CI로 기계화.**
- **규칙을 HTML 주석에 넣지 마라 — Claude Code는 CLAUDE.md의 블록 주석을 컨텍스트에 싣기 전에 제거한다**(공식 문서: "stripped before the content is injected into Claude's context"). 밀도 규약이 3개 repo에서 `CLAUDE.md:3` 주석에 갇혀 있어 개발자들의 AI가 한 번도 읽지 못했고, 그 사이 킷 CLAUDE.md가 300자 초과 21줄까지 부풀었다(2026-08-06 실측 → 본문으로 이전). 주석은 사람용 메모 전용.
- **금지보다 채널링**: 내장 기능은 제한 규칙 대신 킷 스킬·가이드에 용법으로 녹여라.
- **도구·워크플로우 선택은 WebSearch 먼저 + 실증**(추론만으로 정하지 마라). 기각된 방법은 폴백으로도 문서에 안 남긴다.
- **문서 검수법**: 스윕은 "시킨 렌즈"만 본다 → 누적 렌즈 + 페르소나 통독 병행. **블록 삭제는 Edit로만**(sed 범위 삭제 금지 — 인접 내용 유실).
- 부정된 방법 재등장 금지: 고화질 스샷으로 아이콘·문구 잡기 / 한 방법 0건=완료 / 좌표 등분 매핑.
- **사람 보고는 "사람이 할 일" 기준 3구획**(㉠지금 정할 것 ㉡나중에 할 것(명령·시점) ㉢알아만 둘 것) — **빈 칸도 "없음"**. 발견 목록 나열은 보고가 아니다(2026-08-26 실측: 15건 나열 → "난 뭐 어떻게 하라는거야". 같은 날 오너 "무슨말이야" 2회). 규칙 정본 = CLAUDE.md '머지 등급' 증적 ①.
- **판정 규칙을 넓게 잡으면 상시 경고가 되고, 상시 경고는 아무도 안 본다** — 게이트는 "정상 작업에 침묵"이 1순위 요구다. 넓힌 규칙은 회귀 테스트로 오탐부터 확인하라(2026-08-26 tamper-check 2층화: 23건 중 14건 실패 → 알림/실패 분리).
- **게이트의 회귀 테스트 파일은 게이트 자신의 스캔 대상**이다 — 픽스처 문자열이 진짜 위반으로 잡힌다. 우회(토큰 조립)는 다른 오탐을 만들 수 있으니 교환비를 먼저 재라(2026-08-26 실측).
- **합격/불합격을 내는 검사 스크립트는 파이프로 자르지 마라.** ⛔는 출력 **맨 앞**이라 `tail`에 잘리고, **파이프가 종료 코드를 삼킨다**(`cmd | tail` = tail의 0 → `&&` 통과). 그렇게 밀도 실패를 안고 커밋해 CI가 대신 잡았다(2026-08-27). 파이프 없이 돌리고 `echo "EXIT=$?"`, 조용히 하려면 `>/dev/null 2>&1` 후 `$?` — **자르지 말고 버려라.**
- **"확인했다"의 범위를 실제보다 넓게 말하지 마라.** 하루 3회 밟았다(2026-08-27): 잘라 본 검사를 "OK"로 · `/code-review` 안 돌리고 증적에 "fresh-context 리뷰" · repo 규칙을 안 읽고 상태를 "미완"으로 단정. **증적엔 "확인함/못 봄"을 나눠 쓰고, 검증 이름은 실제 실행한 명령 기준으로.** 잘못 적었으면 원 기록을 지우지 말고 **정정을 따로** 남긴다.
- **CI 빨간불은 로그를 열고 판정하라** — `density-check` 실패가 실은 러너의 `Connection reset by peer`(소스 fetch 실패)라 스크립트가 실행조차 안 됐다(2026-08-27). 잡 이름만 보고 원인을 단정하면 오보한다.
- **3배포처 대조는 양방향으로** — `>`(배포처에만=현지 값)만 보면 `<`(정본에만=아직 안 간 규칙)를 놓쳐 배포처가 서로 갈린다(2026-08-27 실측 2건).
- **남의 클론에 `cd`해서 HEAD를 옮기지 마라**(`checkout`·`switch`·`pull`·`reset`) — 별도 워크트리나 웹에서. `session-guard.sh`가 경고하지만 **보조일 뿐**이다: 상대가 잠금에 등록돼 있어야 뜨고 등록은 세션 시작 때만 일어나므로 **훅 배포 전에 시작된 세션은 못 잡는다**(배포 당일 실증). 1차 방어는 규칙이다.

## 3. 프로젝트 지형
- **세 곳**: 킷 `E:\ai-framework\3-starter-kit`(원본 틀=정본) · 파일럿 `E:\ai-framework-pilot`(검증용) · bnsone `E:\bnsone`(실 프로젝트, gitlab.bns.co.kr). 맥에선 경로가 다르니 clone 위치에 맞춰 읽을 것.
- **동기화 규칙**: 순수 프레임워크 파일(`.claude/skills`·`rules`)은 킷이 정본 → 그대로 복사. **CLAUDE.md·`.gitlab-ci.yml`·가이드는 로직·구조만 일치, 각 repo의 채운 값(모듈명·기동명령·스택)은 보존**(통째 덮으면 프로젝트 값 유실). 드리프트 누적 금지.
  - ★**킷 저장소 안에도 사본이 하나 더 있다**: 킷 세션이 실제로 쓰는 스킬은 루트 `.claude/skills/` 뿐이고(`3-starter-kit/.claude/skills/` 는 한 칸 아래라 Claude Code 가 안 읽는다), 지금 거기 있는 건 `/overhaul` 하나다. **킷 원본의 `/overhaul` 을 고치면 루트 사본도 같은 커밋에서 맞춰라** — 2026-09-08 실측: `8d8659f` 가 원본만 3갈래로 넓혀 킷 세션의 `/overhaul` 이 20일 넘게 문서만 보고 있었다. 허용된 현지 차이는 «모델 플로어» 줄 하나뿐이고 **`effort` 핀·`allowed-tools` 는 원본과 같아야 한다**(2026-09-08 e3a52fc 가 원본만 고쳐 하루 만에 또 갈렸다). **저장 워크플로도 같다**: `/overhaul` 이 부르는 `audit-sweep.js` 는 `3-starter-kit/.claude/workflows/` 가 원본, 루트 `.claude/workflows/` 가 킷 세션용 사본 — 같은 커밋에서 맞춰라(2026-09-08 신설).
- **경계 = "제품 개발이냐"지 "bnsone이냐"가 아니다.** bnsone 실사용에서 나온 킷 결함을 고쳐 bnsone에 되반영하는 건 유지보수라 정당(단 실 repo라 remote push는 명시 승인·미커밋 작업자 없음 확인 후).
- **파일럿 단계 규칙**: 실험 산출물은 파일럿에만. bnsone 쓰기 = 명시 경고·승인 후만. **실 팀원(임승민·yhy·kidkidtwo 등) 배정·@멘션 금지 — assignee는 yskim만.** 팀 착수(온보딩) 후 해제.
- **역할**: 공통 개발자 / 개발자 / 기획자·검수. **명명**: 현재=BNSOne(사내용), OfficeOne=향후 제품(기획자 차터는 OfficeOne이라 인식 불일치 있음). ProjectCharter의 STEP=일정(날짜), 프레임워크 STEP=작업순서(별개 축, 의도된 구분).

## 4. 참고
- **이 repo(전체 백업, Private)**: https://github.com/majorTom-cat/ai-framework (`main`). ⚠️Private 유지 — 실명·사내도메인 포함.
- **공개 판정 — 정정(2026-08-06 실측)**: 공개 후보는 `3-starter-kit`만인 건 맞지만 **"사내정보 0건"은 틀렸다.** 정화된 것은 **인프라**다: 서버주소·Harbor·IP·계정·실명·토큰·`bns.co.kr` = 실제 0건. 반면 **프로젝트 코드네임 `bnsone`이 30건 / 20개 파일** 남아 있다 — 내부 이슈·MR 번호(`#6`·`#16`·`!31`), 파이프라인 번호(`#2786`·`#2797~2803`), 사고 이력 문구 포함. `.gitlab-ci.yml`·`CLAUDE.md`·`_reference/ci-needs.md`·스킬 7개·`docs/00_Guide` 3개에 흩어져 있다.
  → **공개 전에 코드네임 치환이 선행 조건**(예: `bnsone` → `<프로젝트>`). 재확인은 킷 폴더에서 `grep -ric bnsone . | grep -v '^./.git/' | grep -v ':0$'` (파일별 건수 + 합계로 검증). 치환 시 이슈·파이프라인 번호도 함께 익명화할지 판단하라 — 번호만으로도 사내 GitLab 활동량이 드러난다.
- **사내 GitLab**: k8s Helm eval 설치본. Pages 켰으나 CI 아티팩트 400 미완(파일럿 #17). 전담 서버 관리자 부재.
- **⌨️ 한글 입력 버그(Windows Claude Code)**: IME 조합 완료 전 키 유실 → 타이핑 시 글자 빠짐. 우회=붙여넣기 or `terminal-ime-proxy`. **맥에선 이 버그 없을 수 있음**(확인해볼 것).
- 프로젝트 상태·작업 이력의 상세는 **`HANDOFF.md`**(이력 로그).
