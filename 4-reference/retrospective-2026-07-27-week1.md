# 회고 기록 — 1주차 (착수 ~ 2026-07-27)

> 프레임워크 내부 기록(4-reference — bnsone/pilot 미배포). 형식 = Leader 가이드 "주 1회 회고"(버릴 것/더할 것/유지할 것 · 액션 1~2 · 가지치기 필수).
> 상태: **초안** — 리더 검토·실행 대기. 액션은 실행 후 다음 회고에서 반영 여부 확인.

## 0. 지난 회고 액션 반영 확인
**첫 정식 회고.** 그동안 회고는 HANDOFF 세션 로그로 비공식 진행(기록 충실, "3질문+액션" 구조는 아님). 이 기록이 그 구조의 시작 — 다음 회고는 여기 액션(특히 /metrics)이 반영됐는지부터 확인한다.

## 🔴 버릴 것 (Stop)
- **"분량을 줄 수로 재는" 습관** — `/start`를 "3~5줄"로 다이어트(#9)했다가 맥락까지 깎여 재조정(#10). 자기 유발 재작업 1회. 기준은 "줄 수"가 아니라 "필요한 부(部)를 각각 간결하게". (#10에 반영됨 — 재발 방지 명시)
- **파일럿 F6 잔재** — `#47 확인 대기` 오기가 파일럿 5파일에 잔존. 리허설 무대라 실전 무영향 → **지금 안 지움**(정리 비용 > 가치), 버릴 후보로만 등록.

## 🟢 더할 것 (Add) — ⚠️ 가지치기: 순증 금지
- 이번 주 이미 6건 추가(#7·#9~#12 + 위치배너·표지판). **지금은 멈출 때** — 새 규칙 후보 일부러 만들지 않음.
- 유일한 실질 추가 = 미래 예약분: 디자인 아이덴티티 정본(design-tokens.md) — bnsone 첫 `/ui` 시안 채택 시 추출. 지금 만들면 과설계.

## 🔵 유지할 것 (Keep)
- **"실전 마찰 → 킷 수리 → 3배포처 동기" 루프** — 이번 주 6회전, 전부 실제 마찰 발단.
- **DD-03 파일럿 선행 검증** — Next.js scaffold의 Docker 함정(husky prepare)을 bnsone Day 1 전에 잡음.
- **전용 카드 규칙·위임 방어·미리보기 게이트** — 전용 카드 규칙이 #47 오기 실제 감지.
- **PowerShell 권한 규칙**(#9 부수) — 리허설 승인 프롬프트 8회 → 0회.

## ✅ 액션 (1~2개, 실행으로)
**액션 1 (최우선): `/metrics` 베이스라인 1회 — bnsone에서.**
- 왜: 회고 원칙("안 쓰는 스킬은 죽인다"·"지난 액션 반영 확인")이 전부 데이터를 요구하는데 **`/metrics`가 아직 0회**. 지표(리드타임·반려율·리버트·반려사유)가 없어 가지치기가 감(感). 베이스라인 1회로 다음 회고부터 데이터 기반 판정 가능.
- 실행: bnsone에서 `/metrics`(기존 스킬, 추가 없음, **부작용 없음 = 카드 안 생김**).

**액션 2 (선택·예약): 다음 회고 = 스킬 가지치기 회고.**
- 액션 1이 데이터를 쌓으면 2~3주 뒤 "18스킬 + 새 규칙(위치배너·표지판·glab)이 bnsone에서 실제 쓰였나 → 안 쓰인 건 죽인다"를 데이터로 판정. 지금은 데이터 없어 불가 — 예약만.

## 한 줄 요약
1주차 = "실전이 프레임워크를 다듬는" 루프 6회전 안착. 지금 필요한 건 더 넣기가 아니라 **`/metrics`로 측정을 시작해 다음 가지치기를 데이터로** 하게 만드는 것.

## 근거 (이력)
- 이번 주 커밋: #6(docs 인수)·#7(glab 여러줄)·#9·#10(/start ×2)·#11(표지판)·#12(scaffold 결함) — bnsone main
- 파일럿 리허설: scaffold Next.js(#55)·assign-module(#57)·/start·표지판 — 전부 통과, 승인 0회
- bnsone 현황(7-27): 카드 #8 설계 게이트 진행(dev jiyeon0426 의견 = Next.js·PG16·6모듈 동의), 파이프라인 초록
